# L4-EG v2.0 预期差量化交易策略 SPEC
## ——公共预期测量、异常识别与可成交后超额收益的经济闭环

> 版本：v2.0（研究/影子盘候选）
>
> 日期：2026-08-20
>
> 适用市场：沪深 A 股普通股票；只做多；T+1；目标退出日不晚于买入后第 1 个交易日；无对手盘时持有期被动延长至实际成交
>
> 核心对象：昨日涨停股、连板股及其非重叠题材簇
>
> 证据状态：机制假说，尚未通过本 SPEC 的验证门；不得将本文档理解为收益承诺

---

## §0 版本结论、边界与术语纠偏

### §0.1 v2 的核心改动

v1 的主要缺陷是把历史条件均值直接称为“市场公共预期”，并默认早盘超预期必然带来后续收益。v2 将策略拆成三个必须分别验证的模型：

```text
M_E：PIT 公共预期估计器
  截止 09:25:05 的公开信息 → 预测 10:00 可观测状态的完整分布

M_S：异常识别器
  10:00 实现值相对 M_E 预测分布的位置 → Surprise 残差

M_A：可交易 Alpha 模型
  Surprise + 控制变量 → 预测 10:00 后、按真实可成交价格计算的净收益
```

只有当 M_A 在严格样本外证明：

\[
\mathbb{E}[r^{net}_{entry\rightarrow exit}\mid S,X] > 0
\]

且置信下界、容量、回撤和真实成交门同时通过，策略才允许下单。

### §0.2 三个不可混淆的概念

| 概念 | v2 定义 | 不代表什么 |
|---|---|---|
| 潜在公共预期 \(E^*\) | 市场参与者在某一时点对未来状态的不可直接观测的共同信念 | 不可被任何单一数据源精确读取 |
| 模型公共预期 \(\hat E^{pub}\) | 用截止时点可公开获得的信息，对 \(E^*\) 的可复现估计 | 不是“市场真实共识”的事实声明 |
| 历史条件基准 \(E^{base}\) | 历史相似样本的条件分布 | 不能单独称为公共预期 |

全文中的“公共预期”均指 \(\hat E^{pub}\)，并必须同时报告预测不确定性。

### §0.3 策略真实身份

L4-EG v2 是一个待验证的：

> **涨停结构条件下，早盘实现状态相对公开信息预测分布发生异常后，交易其后续未完成定价的短周期事件策略。**

它不声称：

1. 涨停梯队是公共预期的唯一测量仪；
2. Surprise 本身必然有收益；
3. 价格达到止损线时一定能够成交；
4. 历史回测能够保证未来盈利。

### §0.4 上线状态

策略状态只有四档：

```text
RESEARCH → SHADOW → CANARY → LIVE
```

新实现默认 `RESEARCH`。未依次通过 §15 的门禁，不得跳级。

---

## §1 经济闭环与可证伪命题

### §1.1 经济闭环

```text
T 日收盘后
  生成 T+1 候选对象和 PIT 特征
          ↓
T+1 09:25:05
  冻结公开信息集 I_09:25，M_E 预测 10:00 状态分布
          ↓
T+1 10:00:00
  读取现实 Y_10，计算分布残差 S
          ↓
T+1 10:00:05
  M_A 预测从真实可成交入场价起算的机会净值、超额值及尾部分位
          ↓
通过 Alpha 门、成交门、风险门
  10:00:10–10:05:00 短时限价执行
          ↓
T+1 收盘至 T+2 收盘
  按预注册退出规则结算
          ↓
记录预测、订单、成交、成本和净收益
  仅在下一次预定训练日进入模型训练；盘中不自适应改参
```

### §1.2 三个独立可证伪命题

| 编号 | 命题 | 失败后果 |
|---|---|---|
| H-E | \(\hat E^{pub}\) 对 10:00 状态的样本外预测同时优于历史无条件基线和仅结构基线 | 公共预期测量失败，停止策略 |
| H-S | 分布残差 S 在不同年份、市场状态中保持可校准、非退化 | 异常测量失败，停止策略 |
| H-A | 在控制普通早盘动量后，S 对可成交后的净收益仍有增量预测力 | “预期差”无独立 Alpha，停止策略或降级为普通动量研究 |

不能用 H-E 成立替代 H-A；预测现实准确不等于能够赚钱。

### §1.3 收益定义

每笔交易同时计算绝对净收益与基准调整净收益：

\[
r^{net}=P_{exit}^{net}/P_{fill}^{gross}-1
\]

\[
r^{alpha}=r^{net}-r^{benchmark}
\]

现金流口径：

\[
CashOut=\sum_k p^{buy}_kq_k+Fee_{buy}
\]

\[
CashIn=\sum_j p^{sell}_jq_j-Fee_{sell}
\]

\[
r^{net}=CashIn/CashOut-1
\]

成交价已经包含价差和盘口冲击，不得再次重复扣除。部分成交按实际股数计算；完全未成交的机会收益和占用资金均记为零。

`benchmark` 使用版本化 `benchmark_map.csv` 将股票上市板块唯一映射到一个可交易宽基指数。基准收益使用该指数在每笔买入、卖出实际成交时刻之后第一笔成交价，按股票现金流权重计算；不扣交易费、不使用事后成分、不允许临时 fallback。指数数据缺失则该交易日 Gate-D 失败。行业/匹配组合只作为稳健性检验，不替代主基准。

下单和上线门同时要求绝对净收益与基准调整收益的样本外经济下界为正，避免只赚取市场 Beta。

---

## §2 交易时钟与信息截止

### §2.1 时钟

| 时点 | 动作 | 可使用的数据 |
|---|---|---|
| T 15:30 | 构建候选池 | 交易所截至 T 收盘已发布数据 |
| T+1 09:14:59 | 冻结隔夜信息 | 截止该秒已公开且已被系统接收的数据 |
| T+1 09:15–09:20 | 观察可撤单竞价 | 仅作为 M_E 特征，不下单 |
| T+1 09:20–09:25 | 观察不可撤单竞价 | 作为 M_E 特征，不下单 |
| T+1 09:25:05 | 冻结 \(I_{09:25}\)，生成 10:00 预测分布 | 不得使用 09:25:05 后数据 |
| T+1 09:30–10:00 | 只采集现实路径 | 不提前使用未完成的 10:00 标签交易 |
| T+1 10:00:05 | 冻结现实并生成 Surprise | 仅使用源时间≤10:00:00且接收、处理完成时间≤10:00:05的数据 |
| T+1 10:00:10–10:05 | 交易窗口 | 使用已生成信号、实时盘口和风控数据 |
| T+1/T+2 | 退出 | 依 §12 |

### §2.2 时间纪律

1. 所有事件保存交易所源时间、系统接收时间、处理完成时间和入库时间。
2. 特征可用时间取四者的最大值。
3. 09:25:05 的模型输入不得包含 09:25:05 后修订的数据。
4. 10:00目标只纳入源时间不晚于10:00:00且在10:00:05前处理完成的记录；迟到记录保留审计但不得回填当次决策。
5. M_S/M_A 的计算预算为5秒，订单最早10:00:10发送；回测还必须叠加实盘日志测得的网络、柜台和交易所确认延迟。
6. 不得以10:00:00或10:00:05的理论价格假设成交。
7. 夜间公告不会被忽略：公告只在系统实际接收并处理完成后进入隔夜特征。

---

## §3 数据契约与生产数据源

### §3.1 必需数据

| 数据域 | 最低粒度 | 必需字段 | 用途 |
|---|---|---|---|
| 证券主数据 | 日/PIT | 代码、上市板块、ST状态、上市日、停复牌、涨跌幅规则 | 合法交易与涨跌停价 |
| 除权除息 | 事件/PIT | 生效日、前收调整、复权因子发布时间 | 价格连续性与涨跌停计算 |
| 日线 | 日 | 未复权 OHLCV、成交额、换手率、流通市值 | 结构与流动性特征 |
| 涨停/炸板/跌停事件 | 逐事件 | 首次/最后触板时间、封板状态、开板次数 | 梯队结构 |
| 集合竞价 | 1秒或更细 | 指示价、匹配量、未匹配量、方向；源/收时间 | 公共预期特征 |
| 逐笔委托/撤单/成交 | 订单级L2 | 订单序号、方向、价量、委托/撤单/成交、源/收/处理时间 | 队列与双边成交重放 |
| 至少五档盘口 | Tick/快照 | 买卖价量、源/收/处理时间 | 实时成交门与L2重建校验 |
| 题材/行业映射 | PIT版本 | 标签、首次可见时间、字典版本 | 非重叠题材簇 |
| 公告 | PIT | 证券、类别、交易所发布时间、接收时间 | 隔夜公开信息 |
| 指数/板块行情 | Tick/分钟 | OHLCV | 基准调整收益 |
| 费率表 | 生效日版本 | 佣金、印花税、过户费 | 历史真实成本 |

### §3.2 数据源原则

1. 交易所授权行情、券商行情或可审计数据商为生产主源。
2. 东方财富、腾讯等公开接口可作交叉验证或研究备源，不作为无 SLA 的唯一生产源。
3. 没有历史竞价和订单级L2委托、撤单、成交数据时，只能研究 H-E/H-S；五档快照不能替代队列数据，不得通过 G5/G7。
4. 所有原始文件只追加、不覆盖，保存 SHA-256、数据许可、抓取程序版本和抓取时间。

### §3.3 数据质量门 Gate-D

任一条件满足，当日禁止新开仓：

- 候选股竞价或连续竞价数据缺失；
- 证券主数据、ST、停牌或涨跌幅规则无法确定；
- 主源与备源的前收、开盘价或涨跌停价不一致；
- 本地时钟与行情源偏差超过 500 毫秒；
- 10:00 前 Tick 序列出现倒序、重复且无法去重；
- 候选股盘口延迟超过 2 秒；
- 当日候选中任一模型必需特征缺失率超过 5%；
- 模型、参数、题材字典或费率版本无法加载。

缺失值不得用未来数据补齐。允许的历史滚动特征缺失必须使用训练期固定的缺失指示器与中位数。

---

## §4 候选池与结构定义

### §4.1 基础集合

T 日收盘后：

\[
ZT(T)=\{i: i\text{ 在 T 日收盘封住涨停}\}
\]

\[
LB_k(T)=\{i\in ZT(T): lbc(i,T)=k\}
\]

`lbc` 必须由逐日连续收盘涨停记录自行重建，数据商字段只作校验。

### §4.2 候选股票

T+1 候选池：

\[
U(T+1)=ZT(T)
\]

合格题材簇定义为：

\[
C^*(T)=\{c:ListedCount(c,T)\ge3,\ |ZT(T)\cap c|\ge2\}
\]

每个 \(c\in C^*(T)\) 的代表 `Leader(c,T)` 必须属于 \(ZT(T)\cap c\)，并按以下顺序唯一选出：

1. 连板数高；
2. T 日首次封板时间早；
3. T 日成交额高；
4. 股票代码字典序小。

`Leader` 只是候选特征，不会把昨日未涨停股票额外加入候选池。

### §4.3 硬排除

以下股票不得进入可交易池：

- ST、退市整理、停牌；
- 上市不足 60 个交易日；
- T+1 涨跌幅规则无法确定；
- 过去 20 日中位成交额低于 P-01；
- T 日收盘价低于 P-02；
- T+1 10:00 时已封涨停且卖一为零；
- 盘口或 Tick 数据不完整；
- 公告显示次日停牌、退市风险或交易规则变化。

流动性约束不得因资金规模较小而删除，只能依据容量验证调整阈值。

### §4.4 非重叠题材簇

题材分类必须满足“同一股票同一日只属于一个交易簇”：

1. 读取在 T 日 15:00 前已公开的标签；
2. 用版本化同义词表标准化标签；
3. 删除“融资融券、深股通、预盈预增”等非产业标签；
4. 对每个股票—标签计算：

\[
score(i,c,T)=\frac{ZTCount(c,T)}{ListedCount(c,T)}\times
\log(1+CoLimit20(i,c,T))
\]

其中：

- `ListedCount(c,T)` 是 T 日15:00时标签为 c 且可上市交易的股票数；
- `ZTCount(c,T)=|ZT(T)\cap c|`；
- `CoLimit20(i,c,T)` 是截至 T 的20个交易日内，i 与至少一只同标签股票同时收盘涨停的交易日数；
- 过去不足20日按实际可用日计数，不年化。

5. 取分数最高标签；并列时取 `ListedCount` 较小者，再按标签字典序；
6. 少于 3 只上市公司的标签合并到申万三级行业；
7. 每日保存映射结果、字典版本和哈希。

任何人工修改只能进入下一版本字典，不能回写历史。

### §4.5 结构特征

所有特征仅用截至 T 收盘数据：

- 连板数、高度、同簇涨停数；
- 一进二、二进三等分层历史晋级率；
- 同簇低一和低二身位股票数；
- 首封时间、开板次数、封板时长；
- 成交额、换手率、流通市值；
- 过去 5/20 日收益和波动；
- 题材簇过去 1/3/5 日涨停广度；
- 全市场涨停、炸板、跌停和昨日涨停收益。

“主线”“中军”“孤狼值得买”“情绪高潮”等叙事词不得作为人工输入。

---

## §5 可复现环境状态

### §5.1 连续环境指标

每天计算：

- \(H\)：最高连板；
- \(J_1\)：昨日首板今日收盘二板率；
- \(J_2\)：昨日连板今日继续收盘涨停率；
- \(ZR\)：炸板数/(涨停数+炸板数)；
- \(DT\)：跌停数；
- \(M\)：昨日涨停股今日收益中位数。

各指标用此前 250 个交易日的经验 CDF 转为 \([0,1]\) 分位数；并列值使用 mid-rank。不得包含当日值拟合出的分布参数。若 `ZR` 分母为零，则 ZR 记为缺失，分位数使用此前窗口中位数并增加 `ZR_MISSING=1`。不足250个完整历史日时环境状态为 `UNAVAILABLE`，策略禁止交易。

\[
Heat_T=median(q(H),q(J_1),q(J_2),1-q(ZR),1-q(DT),q(M))
\]

\[
Trend_T=EMA_3(Heat)_T-EMA_3(Heat)_{T-3}
\]

EMA 使用 \(\alpha=2/(3+1)\)，首个值初始化为首个可用 Heat；但在250日预热完成前不输出交易状态。

### §5.2 五态确定算法

按下列优先级生成候选状态：

1. 若 `q(DT)≥0.90`，或 `J2` 连续两日位于历史 10% 分位以下：`RETREAT`；
2. 否则若 `Heat≥0.75`：`CLIMAX`；
3. 否则若 `Trend≥0.05` 且 `Heat<0.45`：`STARTUP`；
4. 否则若 `Trend≥0.05`：`FERMENT`；
5. 否则若 `Trend≤-0.05` 且 `Heat≥0.30`：`COOLING`；
6. 否则若 `Trend≤-0.05`：`RETREAT`；
7. 否则保持前一状态。

除第1条风险覆盖外，候选状态连续两日相同才切换；等待期间保持原状态。预热完成后的初始状态按规则2–6直接确定；若 `|Trend|<0.05`，则 `Heat≥0.60` 初始化为 `FERMENT`、否则初始化为 `STARTUP`。状态用于模型特征和风险上限，不以主观方式改写。

---

## §6 M_E：公共预期估计器

### §6.1 预测目标

M_E 在 09:25:05 冻结，预测 10:00 状态的条件分布。

股票级目标：

\[
Y^{ret}_{i,10}=VWAP_{09:59:30-10:00:00}/Open_i-1
\]

\[
Y^{touch}_{i,10}=1\{\text{截至10:00触及涨停}\}
\]

\[
Y^{hold}_{i,10}=1\{VWAP_{09:59:30-10:00:00}\ge Open_i\}
\]

簇级目标：

\[
Y^{breadth}_{c,10}=
\frac{\#\{\text{簇内昨日涨停股截至10:00触板}\}}
{\#\{\text{簇内昨日涨停且当日可交易股}\}}
\]

\[
Y^{clusterret}_{c,10}=median(Y^{ret}_{i,10}),\ i\in c
\]

这里的簇内股票集合严格限定为：

\[
V_c(T+1)=ZT(T)\cap c\cap Tradable_{09:25}(T+1)
\]

`Tradable_09:25` 只依据09:25时已经知道的停牌、证券状态和数据可用性确定。分母为零时簇目标缺失，不得填零。

### §6.2 信息集

`I_09:25` 分为三组：

**结构先验**

- §4.5 全部 PIT 特征；
- §5 环境连续指标和状态；
- 股票板块、涨跌幅制度、流动性和波动。

**隔夜公开信息**

- 交易所公告类别 one-hot；
- 截至 09:14:59 的同簇公告数量；
- 对应宽基和行业隔夜可得代理收益；代理数据不可得时使用缺失标志。

**竞价共识代理**

- 最终开盘缺口；
- 09:20–09:25 指示价的时间加权均值、斜率、标准差；
- 最后 60 秒匹配量/过去20日成交量；
- 未匹配买卖量方向及其变化；
- 09:20 前后指示价变化；
- 同簇股票开盘缺口中位数、正开比例和横截面离散度。

竞价字段只有在历史与实时口径一致时才可用于生产模型。

### §6.3 模型族

默认模型必须简单、可解释、可复现：

| 目标 | 主模型 | 概率/区间输出 |
|---|---|---|
| 连续收益 | L2正则分位数回归 | 完整的分段线性条件CDF |
| 二值目标 | L2 正则 Logistic | 校准概率 |
| 簇广度 | Beta-Binomial 回归 | 完整预测分布 |

连续目标固定预测以下分位网格：

```text
Q={0.01,0.025,0.05,0.10,0.20,0.30,0.40,0.50,
   0.60,0.70,0.80,0.90,0.95,0.975,0.99}
```

若预测分位发生交叉，按分位值升序重排。相邻分位点间对 \(F(y)\) 线性插值；下尾从目标合法下界 \(L\) 的 `F=0` 线性连接至 q0.01，上尾从 q0.99 线性连接至目标合法上界 \(U\) 的 `F=1`。股票收益的 L/U 由当日涨跌停价相对开盘价计算；簇收益边界取成员边界的最小/最大值。模型必须保存全部分位点、重排结果和 L/U，因此任意实现得到唯一 CDF。

特征处理：

1. 连续特征按训练窗口中位数和 IQR 标准化；
2. 截尾点固定为训练窗口 1%/99% 分位；
3. 缺失值用训练窗口中位数，并加入缺失标志；
4. 类别未知值映射到 `UNKNOWN`；
5. 正则强度只允许从 §9.4 固定网格在内层训练/验证集选择；
6. 模型不得使用股票名称、未来题材名称或人工结果标签。

v2.0 不允许树模型作为临场挑战者；引入新模型族必须发布新 SPEC 版本并重新经过 final holdout。

### §6.4 公共预期测量资格

M_E 必须同时满足：

- 连续目标相对无条件基线和仅结构基线的 OOS pinball loss 均至少改善 P-35，且按交易日 block bootstrap 的95%置信上界小于0；
- 二值目标相对两种基线的 OOS Brier score 均至少改善 P-35，且95%置信上界小于0；
- 概率预测经训练外校准后 ECE 不超过 P-03；
- 连续目标10%–90%区间的分年覆盖率偏离80%不超过 P-36；
- 移除竞价共识代理后，OOS损失至少恶化 P-35且95%置信下界大于0，证明模型不只是历史均值；
- 模型卡明确写作“公共预期估计器”，不得写作“真实市场预期”。

---

## §7 M_S：异常识别器

### §7.1 连续目标 Surprise

对 M_E 给出的预测 CDF \(\hat F(y\mid I_{09:25})\)：

\[
u=\operatorname{clip}(\hat F(Y_{10}),0.0002,0.9998)
\]

\[
S=\Phi^{-1}(u)
\]

最终将 S 截断到 \([-3.5,3.5]\)，记为 `S_cdf`。

### §7.2 离散目标 Surprise

离散目标不伪装成标准正态 PIT：

- 二值目标使用带符号 deviance residual：

\[
S_{dev}=sign(y-p)\sqrt{2\left[
y\log\frac{y}{p}+(1-y)\log\frac{1-y}{1-p}
\right]}
\]

其中 \(0\log0=0\)，p 截断到 \([0.001,0.999]\)。

- Beta-Binomial 广度目标使用 Pearson residual：

\[
S_{pearson}=\frac{y-\mathbb E[Y]}{\sqrt{Var(Y)}}
\]

并截断到 \([-3.5,3.5]\)。二值和计数残差只在各自类型内解释，不与 `S_cdf` 假定同分布。

### §7.3 Surprise 向量

每只候选股形成：

```text
S_cdf_stock_ret
S_dev_stock_touch
S_dev_stock_hold
S_pearson_cluster_breadth
S_cdf_cluster_ret
```

候选不属于 \(C^*(T)\) 时两个簇残差记为缺失并加入缺失标志。联合异常尾概率按“核心指标可用数量”分层建立训练期经验分布；不得把缺失当零。除此之外，不再用手工加权得到一个“万能 GAP_z”。M_A 分别使用这些特征并学习其边际作用。

### §7.4 异常识别资格

S 必须在 OOS 数据中满足：

- 连续目标分年 `S_cdf` 均值绝对值不超过 P-04、标准差位于 P-05；
- 连续目标10%–90%预测区间覆盖率满足 P-36；
- 二值目标 ECE 不超过 P-03、Brier score 通过 §6.4；
- Beta-Binomial 的分年均值预测误差不超过 P-37，80%预测区间覆盖偏差不超过 P-36；
- 模型失准时只触发降级/停机，不允许盘中重估分布。

---

## §8 M_A：可成交后 Alpha 模型

### §8.1 决策时点与标签起点

现实冻结及模型决策时点固定为10:00:05，计算预算5秒；订单最早10:00:10进入市场。

研究标签不是10:00前涨幅，而是固定行为策略 \(\pi_0\) 的订单级可实现价值：

1. 对 §4 每一个候选、每一个交易日，不依赖 M_A，按 §11 的相同规则模拟订单；目标金额为 P-38 与 §11.2 三项容量上限的最小值，低于当日一个最小买入单位则记 `NO_FILL`；
2. 默认目标退出固定为 T+2 09:35，按 §12 卖出规则逐笔重放；
3. 定义 `fill_fraction=实际买入股数/目标股数`；
4. 完全未成交时 `V_net=V_alpha=0`，部分成交按实际现金流计算：

\[
V_{net}=fill\_fraction\times r^{net}
\]

\[
V_{alpha}=fill\_fraction\times r^{alpha}
\]

5. 同时保留 `P_fill`、条件成交收益和实际退出延迟作为辅助标签；
6. M_A 的主目标是每次候选机会的 `V_net` 与 `V_alpha`，而不是只在成交样本上拟合收益。

这使 `NO_FILL` 进入策略价值，避免只选择“事后成交”的样本。实际组合回测仍须按 §11.5 联合重放，不能把单票机会价值直接相加。

### §8.2 Alpha 特征

- §7 Surprise 向量；
- M_E 的预测中位数、区间宽度和校准版本；
- 开盘缺口、10:00前收益、成交量、波动率；
- 题材广度和环境连续指标；
- 市值、流动性、涨跌幅制度；
- “普通早盘动量基线”所需全部控制变量。

加入开盘缺口和10:00前收益是强制要求，用于检验 Surprise 是否超越普通追涨。

### §8.3 默认模型

主模型：

1. Elastic-Net 回归预测 \(V_{net}\) 与 \(V_{alpha}\)；
2. Logistic 回归预测 `fill_fraction>0` 及 `V_net>0`；
3. 分位数回归预测机会价值10%分位 \(Q^{V}_{0.10}\)；
4. 尾部损失定义为正数：

\[
TailLoss=-\mathbb E[V_{net}\mid V_{net}\le Q^{V}_{0.10}]>0
\]

若条件均值非负，则 TailLoss 取 P-39 的分母下限。

模型选择目标是在验证集最大化“扣费后每次机会期望”，但必须满足 §15 风险约束。不得仅以准确率或胜率选模型。

### §8.4 信号门

仅当全部条件满足时生成 `LONG_CANDIDATE`：

1. 两个正向核心异常 `S_cdf_stock_ret`、`S_pearson_cluster_breadth` 的联合经验尾概率 `p_joint` 不超过 P-06；`p_joint` 由训练期“每日两者最大正向分位”的经验分布一次性确定，因此已校正同日多目标选择；
2. 预测机会价值 \(\hat V_{net}\ge P-07\)；
3. 预测基准调整机会价值 \(\hat V_{alpha}\ge P-08\)；
4. 预测机会价值10%分位 \(Q^V_{0.10}\ge P-09\)；
5. \(P(V_{net}>0)\ge P-10\)，且预测成交概率不低于 P-40；
6. M_E、M_S、M_A 的 manifest 状态为 `APPROVED`、训练日距当前不超过31个自然日，且没有任何未解除 Gate；
7. §11 成交门和 §13 风险门通过。

阈值只能通过嵌套 walk-forward 在训练/验证集确定，并在测试段和实盘版本中冻结。

### §8.5 增量 Alpha 检验

必须使用同一算法族、正则网格、训练样本和调参预算比较：

```text
A0：环境、流动性、结构和10:00现实值；不含M_E输出与Surprise
Araw：A0 + M_E分位、现实值×预测中位数交互和固定三次样条
A1：Araw + Surprise向量
```

完整策略相对 A0 的 OOS `V_net` 与 `V_alpha` 增量必须同时满足：聚类/块自助95%置信下界大于0，且至少 P-41 的外层折同号。Araw 与 A1 的差异单独报告；由于 Surprise 是现实值与预测分布的确定性变换，不把 A1 对 Araw 的改善夸大为新增信息。若完整策略不优于 A0，则降级为普通早盘动量，不得继续使用 L4-EG 名称。

---

## §9 模型训练与版本管理

### §9.1 训练日程

- 正式模型只在每月首个交易日前的离线窗口重训；
- 训练使用该日之前已经完成并可获得标签的数据；
- 所有尚未实际退出成交或现金结算的标签从训练集剔除，并额外保留至少2个交易日 embargo；
- 月内不因短期盈亏自动改参；
- 漂移只触发 `DEGRADED` 或 `HALT`，不得触发盘中自适应重训。

### §9.2 Walk-forward 协议

开发期采用滚动 prequential walk-forward，每个外层折：

```text
训练：至少 500 个交易日
隔离：按事件实际标签结束日 purge，并额外 embargo 2 个交易日
验证：随后 60 个交易日
隔离：按事件实际标签结束日 purge，并额外 embargo 2 个交易日
测试：随后 60 个交易日
步长：60 个交易日
```

训练窗为扩展窗：已完成的旧测试折可以在下一折成为历史训练数据，但任何协议、特征或参数不得因查看旧测试结果而修改；因此这些折称为 sequential OOS。另保留最后250个交易日为一次性 final holdout，开发期间禁止查看。

每个边界按每条记录从特征起点到**实际退出成交日**的标签区间 purge；连续跌停导致的延长持有同样纳入。验证后可用训练+验证重拟合，但超参数必须是在该验证段冻结的值。

### §9.3 时间序列 OOF 与模型链隔离

M_A 训练库中的每一条 M_E 分布、校准概率和 Surprise，必须由严格早于该记录的 rolling-origin 模型生成：

```text
对候选日 d：
  M_E训练集标签实际结束日 < d
  校准器训练集标签实际结束日 < d
  生成 d 的 OOF预测与Surprise
```

不得用在记录 d 上拟合过的 M_E 或校准器生成 M_A 特征。验证、测试和 final holdout 分别使用只在其之前数据拟合的模型。M_A 自身的验证/测试也遵守相同边界。没有 OOF 链的记录不得进入 M_A。

### §9.4 固定搜索空间与研究注册

v2.0 不优化 §16 的人工交易阈值；先验值即首轮冻结值，失败则拒绝 v2.0并通过新版本提出修改。唯一允许的模型搜索空间：

```text
Logistic C={0.01,0.1,1,10}
Elastic-Net alpha={1e-4,1e-3,1e-2,1e-1}
Elastic-Net l1_ratio={0,0.25,0.50,0.75,1}
Quantile L2 alpha={1e-4,1e-3,1e-2,1e-1}
```

选择指标依次为验证集 `V_alpha`、`V_net`、较小模型自由度；并列容差为1e-8。题材字典、特征、退出、阈值、模型挑战者和全部尝试在读取 final holdout 前写入只追加研究注册表。final holdout 只运行一次。

### §9.5 模型工件

每个模型版本必须包含：

- `model_id`、训练截止时间、代码 commit；
- 数据快照 ID 与 SHA-256；
- 特征清单、顺序、类型和转换参数；
- 模型系数/权重；
- 概率校准器；
- 参数表版本；
- Python及依赖锁文件哈希；
- OOS 模型卡；
- 创建者和批准状态。

实盘加载时校验所有哈希，不一致则禁止交易。

---

## §10 信号、冲突与状态机

### §10.1 信号状态

```text
WATCH
→ EXPECTATION_FROZEN
→ SURPRISE_READY
→ LONG_CANDIDATE
→ ORDERABLE / REJECTED
→ SENT
→ PARTIAL / FILLED / NO_FILL / CANCELLED
→ EXITED
```

每次状态变化保存事件时间和理由码。

### §10.2 排序

多个候选竞争仓位时，按以下分数降序：

\[
RankScore=
\frac{\min(\hat V_{net},\hat V_{alpha})}
{\max(TailLoss,P\text{-}39)}
\]

`TailLoss` 已基于扣费后的 \(V_{net}\)，分母不得再次加成本。不得按不同定义混合的 Surprise 绝对值排序。

### §10.3 同簇冲突

同一题材簇最多选择 RankScore 最高的一只。未成交后不得自动追逐簇内下一只，除非下一只在10:00:05已经独立通过全部信号门。

---

## §11 真实成交与回测成交模型

### §11.1 入场规则

1. 只允许在 10:00:10–10:05:00 下单；
2. 禁止集合竞价买入；
3. 禁止排队购买已经封住涨停且卖一为零的股票；
4. 只有买一、卖一同时存在时才定义中间价，否则拒单；
5. 首单为可撤限价单，理论价格为 `mid×(1+P-11)`，再按证券主数据中的最小价位向下取整，并限制在涨停价和当时有效价格笼子以内；
6. 目标股数按证券在当日生效的最小买入单位向下取整；不得统一假设100股，科创板等特殊规则由PIT证券主数据提供；
7. 订单有效期 P-12 秒；未成交部分发撤单；
8. 撤单确认前发生的成交必须计入；只有收到撤单确认后才能重挂；
9. 最多重挂 P-13 次，每次重新通过价格、盘口和风险门；
10. 10:05后剩余数量取消，不追单。

### §11.2 实时成交门

订单金额必须同时满足：

\[
OrderValue \le P_{14}\times ADV20
\]

\[
OrderValue \le P_{15}\times Turnover_{09:30-10:00}
\]

\[
OrderValue \le P_{16}\times AskDepth_{5}
\]

并满足：

- 预计单边滑点不超过 P-17；
- 买一卖一价差不超过 P-18；
- 非涨停无卖盘状态；
- 最新盘口年龄不超过2秒；
- 预计成交后单股风险不超过 §13 上限。

### §11.3 历史成交仿真

历史回测使用订单级 L2 逐笔重放：

1. 按实盘测得的“策略计算→网络→柜台→交易所”延迟，将模拟订单注入订单流；缺乏实盘样本时采用 P-46 压力延迟；
2. 使用委托序号重建同价队列，模拟订单排在注入时该价格队尾；
3. 后续成交和撤单按序号减少队首数量，只有前方数量耗尽后模拟订单才能成交；
4. 撤单申请在历史延迟后生效，期间成交仍计入；
5. 部分成交按实际数量计算，未成交记为 `NO_FILL`；
6. 不得使用分钟最低价、VWAP或收盘价反推成交。

只有五档快照而没有委托序号时，替代模型标记 `RESEARCH_ONLY`；P-19 折扣只用于敏感性展示，不能通过 G5/G7。

### §11.4 成本

按交易日生效的真实费率逐项计算，包括最低佣金规则。基准回测直接使用订单重放成交价，成交价已包含价差和冲击，不另扣冲击。

压力测试另算，不覆盖基准结果：

\[
StressAddon=\max(0,\ P\text{-}20-ObservedImplementationShortfall)
\]

其中 `ObservedImplementationShortfall` 是成交均价相对订单到达时中间价的不利偏离；压力结果在现金流中额外扣 `StressAddon`。同时测试费率和 StressAddon 的1.5倍、2倍，明确标记为压力场景。

### §11.5 组合级联合重放

外层验证、final holdout 和容量测试必须按日联合重放全部候选：

1. M_A 先生成排序，风险模块产生目标订单；
2. 所有订单按实盘发送顺序注入同一订单流；
3. 每次部分成交后立即更新现金、总仓位、题材和风险预算；
4. 后续订单不得使用已被前序订单占用的现金或盘口数量；
5. 同一策略的两张订单不得重复获得同一笔历史对手成交；
6. 策略收益以组合净值序列为准，单票 \(\pi_0\) 标签只用于训练。

---

## §12 退出规则

### §12.1 新买仓位

因 T+1 限制，T+1 新买仓位不能当日卖出。v2.0 的默认目标退出固定为 T+2 09:35:00，不参与模型选择。

### §12.2 已可卖仓位的风险退出

从 T+2 开始，任一条件触发即尝试卖出：

- 开盘后可成交价格相对成本亏损达到 P-21；
- 模型指定退出时点到达；
- 股票或题材触发数据/合规风险；
- 组合触发 §13 风险减仓。

止损价只是卖出触发线，不是最大损失保证。若跌停无买盘，记录未成交和实际尾部损失，不得按止损价回填。

### §12.3 卖出执行

目标退出和风险退出采用同一确定性规则：

1. 触发时若买一存在，按买一价提交卖出限价单；按最小价位取整并遵守价格笼子；
2. 10秒未全成则撤单，收到撤单确认后按最新买一重挂，最多3次；
3. 仍未全成的剩余数量，以当日跌停价提交可持续至收盘的限价单；实际成交仍按市场最优可得价；
4. 卖出侧使用与买入侧相同的订单序号、队列、延迟、撤单在途和部分成交重放；
5. 历史最小卖出单位、零碎股一次性卖出规则从PIT证券主数据读取；
6. 风险退出优先于价格优化。

T+2 是**目标退出日**而非保证终止日。若跌停无买盘，剩余仓位每日从开盘集合竞价开始按当日跌停价申报，直至实际成交、停牌处置或退市现金结算。报告同时保存 T+2 收盘盯市收益、最终实现收益、额外持有日数和风险资本占用；不得截断尾部样本。

“涨停开板即卖”不属于 v2.0 默认退出规则。

---

## §13 组合与风险

### §13.1 仓位上限

- 单股初始仓位不超过 NAV 的 P-22；
- 单日新开仓不超过 NAV 的 P-23；
- 组合总多头市值不超过 NAV 的 P-24；
- 同一题材簇不超过 NAV 的 P-25，且最多1只；
- 最大同时持仓数 P-26；
- `RETREAT` 状态禁止新开仓；
- `COOLING` 状态仓位上限乘 P-27。

实际仓位取模型仓位、流动性容量和上述上限的最小值。

### §13.2 风险预算仓位

目标数量：

\[
Shares=
\left\lfloor
\frac{NAV\times RiskBudgetPerTrade}
{Price\times TailLoss}
\right\rfloor_{LotSize(i,T)}
\]

`LotSize(i,T)` 来自PIT证券主数据。`TailLoss` 是正数，至少取：

- M_A 预测尾部损失；
- OOS中相同环境状态、同涨跌幅制度、同流动性五分位事件的10% Expected Shortfall；该单元少于 P-42 个样本时逐项去掉环境、再去掉流动性回退，并记录层级；
- 一个跌停板压力损失；

三者最大值。主板、创业板、科创板按各自涨跌幅制度分别压力测试。

### §13.3 组合停机

任一条件触发，停止新开仓：

- 当日数据门失败；
- 滚动20笔实盘成交的平均滑点超过模型值 P-28；
- 最近 P-29 个已成熟机会的 `|mean(V_net-\hat V_net)|` 超过 P-30；
- 实盘累计回撤超过 P-31；
- 单日净值损失超过 P-32；
- 模型输入分布 PSI 超过 P-33；
- 发生未建模的交易规则变化。

停机后只能经离线复核和新模型版本重新启用。

---

## §14 回测与统计检验

### §14.1 样本与偏差控制

必须做到：

- 证券池包含退市股票；
- ST、板块、涨跌幅制度按历史时点还原；
- 题材标签和公告按首次可用时间还原；
- 复权只用于收益连续性，订单价格使用当时真实未复权价格；
- 同日同簇信号视为相关事件；
- 标签重叠时按交易日和题材双向聚类；block bootstrap 的块长固定为5个交易日；
- 所有研究尝试登记，避免只报告成功版本。

### §14.2 对照组

至少比较：

1. 不交易；
2. 同环境、身位、流动性和开盘涨幅的随机可交易股票；
3. §8.5 定义的普通10:00早盘动量 A0；
4. 仅梯队结构、不含竞价特征；
5. 仅竞价特征、不含梯队结构；
6. 完整 L4-EG v2。

随机对照按交易日重抽样1000次，随机种子固定为20260820，并保持每日信号数量、目标金额、实际成交规则和持仓期限一致。

### §14.3 统计方法

- 收益置信区间使用5交易日 block bootstrap、10000次、种子20260820；
- 回归标准误按交易日和题材双向聚类；
- 多信号、多退出、多模型试验使用 BH-FDR；
- 报告 effect size、置信区间和经济显著性，不只报告 p 值；
- 报告所有外层折，不得只给合并结果；
- 计算 deflated Sharpe 或等价的多重尝试修正指标。

### §14.4 必报指标

- 信号数、下单数、成交数、成交率；
- 部分成交率、平均等待时间；
- 毛收益、净收益、基准调整收益；
- 胜率、盈亏比、单笔期望及95%置信区间；
- 最大回撤、Expected Shortfall、最长连续亏损；
- 换手、佣金、税费、滑点、冲击占毛收益比例；
- 分年度、环境、板块、涨跌幅制度、流动性分组结果；
- A0 与 A1 的增量收益；
- 容量曲线和成本1.5倍/2倍压力结果；
- 无法成交和连续跌停案例。

---

## §15 验证门

| 门 | 内容 | 必须满足 |
|---|---|---|
| G0 复现 | 独立环境从原始快照运行 | 特征、信号、订单逐条一致 |
| G1 PIT | 随机抽查至少100个字段 | 无未来数据，时间戳链完整 |
| G2 M_E | 公共预期估计器样本外质量 | 通过 §6.4 全部条件 |
| G3 M_S | Surprise 校准 | 通过 §7.4 全部条件 |
| G4 增量Alpha | 完整策略对 A0 | `V_net`和`V_alpha`增量的95%下界均>0；至少P-41外层折同号 |
| G5 可成交净收益 | 订单级联合重放、全成本 | 每机会`V_net`、`V_alpha`及组合日收益均值的95%下界全部>0 |
| G6 稳定性 | 分年度/状态/板块 | 最大单年正P&L贡献≤P-43；最大单簇贡献≤P-44；至少4个自然年度有样本 |
| G7 尾部与容量 | 压力成本、跌停、参与率 | 目标资金不超过验证容量1/3，风险预算内 |
| G8 影子盘 | 至少60个交易日且至少30个通过§8.4的信号 | 离线/在线信号100%一致；成交率误差≤P-45；平均滑点超模≤P-28 |
| G9 Canary | P-34 的小风险预算，至少20次实际成交 | 订单状态100%可核对；未触发停机；净/超额收益只作监测、不降低G5标准 |

任何门失败都不得进入下一状态。“影子盘收益同号”不能替代成交、校准和样本量要求。

---

## §16 参数登记表

下表数值均为待验证先验，不是已证明最优值。市场制度常数、时间戳和统计协议常数不属于可调交易参数。

| 编号 | 参数 | v2先验 | 作用 |
|---|---|---:|---|
| P-01 | 20日中位成交额下限 | 5000万元 | 硬流动性过滤 |
| P-02 | 最低股价 | 2元 | 微价股过滤 |
| P-03 | ECE上限 | 0.05 | M_E校准 |
| P-04 | S分年均值绝对值上限 | 0.20 | 残差漂移 |
| P-05 | S分年标准差范围 | [0.70,1.30] | 残差退化 |
| P-06 | 联合正向异常尾概率 | ≤0.05 | 已校正双核心目标 |
| P-07 | 预测绝对机会价值下限 | 0.40% | Alpha门 |
| P-08 | 预测基准调整机会价值下限 | 0.25% | Alpha门 |
| P-09 | 机会价值10%分位下限 | -2.50% | 尾部门 |
| P-10 | 正收益概率下限 | 0.55 | 概率门 |
| P-11 | 首单限价偏离中间价 | 0.30% | 追价限制 |
| P-12 | 单次订单有效期 | 20秒 | 执行 |
| P-13 | 最大重挂次数 | 2 | 执行 |
| P-14 | 单笔/ADV20 | 0.10% | 容量 |
| P-15 | 单笔/前30分钟成交额 | 1.00% | 时段容量 |
| P-16 | 单笔/五档卖盘 | 10.00% | 盘口容量 |
| P-17 | 预计单边滑点上限 | 0.30% | 成交门 |
| P-18 | 最大买卖价差 | 0.50% | 成交门 |
| P-19 | 快照替代模型成交量折扣 | 50% | 仅研究敏感性，不用于过门 |
| P-20 | 最低单边压力滑点 | 0.15% | 成本压力 |
| P-21 | 可卖仓位风险退出触发 | -7.00% | 卖出触发，非损失保证 |
| P-22 | 单股NAV上限 | 3.00% | 集中度 |
| P-23 | 单日新仓NAV上限 | 9.00% | 日风险 |
| P-24 | 总多头NAV上限 | 18.00% | 总敞口 |
| P-25 | 同簇NAV上限 | 3.00% | 题材共振 |
| P-26 | 最大持仓数 | 6 | 运维/分散 |
| P-27 | COOLING仓位乘数 | 0.50 | 环境风险 |
| P-28 | 实盘滑点超模容忍 | 0.20% | 停机 |
| P-29 | 校准监测窗口 | 30个信号 | 漂移 |
| P-30 | 在线机会价值平均误差上限 | 0.50% | 停机 |
| P-31 | 实盘累计回撤上限 | 8.00% | 停机 |
| P-32 | 单日净值损失上限 | 2.00% | 停机 |
| P-33 | PSI上限 | 0.25 | 特征漂移 |
| P-34 | Canary总NAV上限 | 3.00% | 小规模验证 |
| P-35 | M_E最小相对损失改善 | 2.00% | 基线与消融门 |
| P-36 | 80%区间覆盖率最大偏差 | 5个百分点 | 连续/计数校准 |
| P-37 | Beta-Binomial分年均值误差 | 5个百分点 | 计数校准 |
| P-38 | 训练行为策略固定名义金额 | 10万元 | 全候选统一订单标签 |
| P-39 | TailLoss/排序分母下限 | 0.25% | 防止除零和虚高排序 |
| P-40 | 最低预测成交概率 | 0.50 | 成交门 |
| P-41 | 外层折同号比例 | 60% | 增量稳定性 |
| P-42 | ES单元最小样本 | 100 | 尾部估计回退 |
| P-43 | 最大单年正P&L贡献 | 50% | 年度集中度 |
| P-44 | 最大单题材簇正P&L贡献 | 30% | 题材集中度 |
| P-45 | 影子盘成交率绝对误差 | 10个百分点 | 仿真一致性 |
| P-46 | 无实盘延迟样本时压力延迟 | 1000毫秒 | 订单注入 |

参数状态：

```text
PRIOR → CALIBRATED → FROZEN → RETIRED
```

任何参数变更产生新版本，并从 walk-forward 验证重新开始；不得回改旧回测。

---

## §17 每日运行手册

### §17.1 T日收盘后

```text
□ 拉取并固化原始数据，生成哈希
□ 重建涨停、炸板、连板和证券状态
□ 运行 Gate-D
□ 生成非重叠题材簇与候选池
□ 计算结构、环境和流动性特征
□ 输出候选清单、排除理由和模型版本
□ 不生成买入结论
```

### §17.2 T+1 09:14:59–09:25:05

```text
□ 固化隔夜公告和代理行情
□ 采集竞价轨迹
□ 09:25:05 冻结 I_09:25
□ M_E 输出每个10:00目标的预测分布
□ 保存输入哈希、预测分位和模型ID
□ 不进行竞价买入
```

### §17.3 T+1 09:30–10:00

```text
□ 只读取现实路径
□ 10:00:05仅用“源时间≤10:00:00且处理完成≤10:00:05”记录固化Y_10
□ M_S 生成逐目标 Surprise
□ M_A在5秒预算内生成机会净值、超额值和尾部分位预测
□ 信号门失败则记录理由，不交易
```

### §17.4 T+1 10:00:10–10:05

```text
□ 运行成交门和组合风险门
□ 按 RankScore 分配风险预算
□ 下可撤限价单
□ 记录每次盘口、订单、撤单和成交
□ 10:05 撤销未成交余量
```

### §17.5 收盘与退出

```text
□ 核对成交、费用和持仓
□ 生成规定退出订单
□ 记录实际退出价和未成交风险
□ 标签成熟后写入只追加研究库
□ 月内不自动重训或调参
```

---

## §18 日志与审计

每个候选必须有唯一 `decision_id`，并保存：

- 日期、证券、题材簇及映射哈希；
- 原始数据快照和特征哈希；
- M_E 分布预测；
- 10:00实现值与每个 Surprise；
- M_A 所有预测和信号门结果；
- 风险、流动性和拒绝理由；
- 决策时盘口；
- 订单、撤单、部分成交和费用；
- 退出原因、实际净收益和基准调整收益；
- 代码 commit、环境和参数版本。

无完整 `decision_id` 链的交易记为流程事故，不得纳入策略业绩。

---

## §19 失效模式

| 失效模式 | 识别 | 处理 |
|---|---|---|
| 公共预期代理失真 | M_E校准/覆盖率恶化 | 停止新仓，离线重训 |
| Surprise退化 | S均值、方差、PIT漂移 | 模型降级或停机 |
| Surprise无增量Alpha | A1不优于A0 | 停止使用预期差命名与策略 |
| 题材标签后见偏差 | PIT审计失败 | 废弃相关回测版本 |
| 竞价操纵 | 竞价轨迹异常、区间变宽 | 由M_E吸收不确定性；不人工拍板 |
| 涨停不可成交 | 卖一为零/队列过长 | 不排板，记录NO_FILL |
| 跌停无法止损 | 买一为零 | 记录实际尾损，持续申报 |
| 数据源失效 | Gate-D | 当日不新开仓 |
| 成本/拥挤上升 | 实盘滑点与成交率漂移 | 停机，不盘中提高门槛 |
| 市场制度变化 | 规则主数据变化 | 策略HALT，新版本重验 |
| 模型选择过拟合 | 外层折不稳定 | 缩减模型和特征，不解释性补故事 |

---

## §20 最小复现包

一个合格实现必须产出：

```text
config/
  parameters_v2.yaml
  fee_schedule.csv
  feature_schema.yaml
  topic_dictionary_<version>.csv
data_manifest/
  raw_snapshot_manifest.json
models/
  ME_<id>/
  MA_<id>/
reports/
  walk_forward_<run_id>.html
  fills_<run_id>.parquet
  decisions_<run_id>.parquet
  model_card_<id>.md
audit/
  pit_samples_<run_id>.csv
  reproduction_<run_id>.json
```

复现命令必须只接受：

```text
data_snapshot_id + code_commit + config_version + run_id
```

在相同计算环境下应生成相同的特征、预测、信号和模拟订单。随机过程必须固定种子；统计置信区间的种子写入报告。

---

## §21 最终上线判定

L4-EG v2 只有在以下链条全部成立时才构成完整策略：

```text
公开信息能够对10:00状态形成稳定的样本外预测分布
                    ↓
现实相对预测分布的位置能够被统一、可校准地度量
                    ↓
该异常在控制早盘动量后仍预测10:00后的净超额收益
                    ↓
收益能够在真实盘口、T+1、成本和容量约束下成交
                    ↓
影子盘与Canary验证没有破坏上述关系
```

其中任一箭头缺失，只能称为研究假说，不能称为已完成的量化交易策略。

---

## 附录 A：单日决策伪代码

```python
def run_day(trade_date, model_bundle, config):
    assert data_gate_passed(trade_date)

    candidates = build_candidates_asof(previous_trade_day(trade_date))
    candidates = apply_hard_exclusions(candidates)

    info_0925 = freeze_features(candidates, asof=f"{trade_date} 09:25:05")
    expectation = model_bundle.ME.predict_distribution(info_0925)
    persist_expectation(expectation)

    wait_until(f"{trade_date} 10:00:05")
    reality = freeze_reality(
        candidates,
        source_time_lte=f"{trade_date} 10:00:00",
        processed_time_lte=f"{trade_date} 10:00:05",
    )
    surprise = distributional_residual(expectation, reality)

    alpha_features = make_alpha_features(info_0925, reality, surprise)
    alpha = model_bundle.MA.predict(alpha_features)
    signals = apply_frozen_signal_gates(alpha, surprise, config)

    wait_until(f"{trade_date} 10:00:10")
    orderable = [
        s for s in signals
        if live_execution_gate(s) and portfolio_risk_gate(s)
    ]

    for signal in rank_and_allocate(orderable):
        execute_short_lived_limit_order(signal, cutoff="10:05:00")

    reconcile_orders_and_positions()
    schedule_frozen_exits()
    append_audit_log()
```

## 附录 B：v1问题—v2修复映射

| v1问题 | v2修复 |
|---|---|
| 历史均值冒充公共预期 | 潜在预期、模型预期、历史基准三分；M_E必须优于基线 |
| GAP直接推出盈利 | 增加M_A，直接预测真实入场后的净/超额收益 |
| 二值、计数、连续变量混用z分 | 连续CDF残差、二值deviance残差、计数Pearson残差分离 |
| 题材晋级只数受簇规模影响 | Beta-Binomial与广度比例 |
| bucket稀疏、频繁回退 | 正则化模型、连续特征和预测区间 |
| 主线/高潮等人工标签 | PIT特征和确定性状态算法 |
| A级信号需R2却在竞价买 | 禁止竞价买入；10:00后统一决策 |
| 状态滞回与案例冲突 | 明确优先级、风险覆盖和两日确认 |
| 无法成交仍计收益 | 盘口重放、队列最后、NO_FILL |
| 滑点上限代替成本模型 | 真实费率、盘口冲击和压力成本 |
| -7%被误作最大损失 | 明确仅为触发线，跌停按实际损失 |
| 参数冻结与动态调参冲突 | 漂移只停机；变更必须新版本重验 |
| 20日影子盘过短 | 至少60日且至少30个通过§8.4的信号 |
| 双实现等同策略有效 | 复现门与经济有效性门完全分开 |

---

*本文档定义的是可复现、可执行和可证伪的研究及交易流程。只有通过全部验证门后，才允许以冻结版本进入有限风险实盘。*
