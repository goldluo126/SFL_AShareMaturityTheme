# SFL-2 v1.0 产业时钟·中军趋势策略 SPEC
## ——政权交接的可测量定义、可回测验证与一致性风控

> 版本：v1.0（研究候选；由 v0.1 框架经 critical review 与 RCA 重构）
>
> 日期：2026-08-25
>
> 适用市场：沪深 A 股；只做多；周度决策时钟＋财报事件插队；目标持有期 2–8 个季度
>
> 家族定位：SFL 第三时钟 IndustrialClock；与 L4-EG v2（日级）、SFL-1（周级）台账物理隔离
>
> 证据状态：机制假说。未通过 §12 验证门前，本策略状态为 `RESEARCH`，不得实仓。不构成投资建议。

---

## §0 版本结论与策略身份

### §0.1 对 v0.1 的判定

v0.1 的策略思想有部分学术与实证基础（PEAD、基本面动量、机构化与价格趋势的关联、动量生命周期），"不预测产业化、只测量定价权交接"的立场方向正确。但 v0.1 不构成可回测、可复现的策略，五个根本缺陷：

1. **成员快照非 PIT**：概念板块成分是数据商事后维护的，历史回测用今天的成分表必然引入"入选即幸存"的未来函数——这是全框架最大的单点失效；
2. **样本量与参数量失配**：季级策略、约 5–10 个手工挑选的案例，却要校准 14 个参数并做校准/验证分离——统计上不可能；
3. **信号时钟混杂**：B1–B6 混合日频、月频、季频与事件频，"≥4/6 持续 ≥10 日"在数学上无定义；
4. **交接确认 ≠ 正期望收益**：与 L4-EG v1 相同的缺口——识别出政权交接，不等于确认后入场扣除成本与对照后仍有超额；
5. **风控数学自相矛盾**：4 题材 ×30% 上限可达 120% NAV；单题材容忍 −30% 回撤与组合 −20% 预算冲突；E5 破位线与 −30% 容忍规则冲突。

### §0.2 v1.0 的核心改动

```text
M_R：政权状态测量器
  PIT 成员快照 + 行为数据 → 题材级六维状态向量（统一日频状态变量）
          ↓
M_C：交接确认器
  自适应法定人数 + 滞回 → CONFIRMED / REVOKED（可判定性显式化）
          ↓
M_Q：中军资格与排序器
  版本化正宗性词典 + 冻结等权评分 → 持仓梯度
          ↓
H-A 增量检验：确认后组合相对趋势基线与动量匹配对照的净超额
          ↓
一致性风控：总仓、题材、个股、回撤阶梯在联合压力下无矛盾
```

### §0.3 策略真实身份

SFL-2 v1.0 是一个待验证的：

> **以 PIT 题材成员快照为基础，用行为数据识别题材定价权从短线资金向配置型资金移交，确认后按季报验证节奏持有簇内高纯度、高正宗性中军的低频趋势策略。**

它不声称：

1. 能预测哪个题材会产业化；
2. 六信号是政权交接的充分证明（只是可测量代理）；
3. 案例库中 CPO/新能源等成功案例可外推（它们是幸存者样本，仅作机制说明，不作统计证据）；
4. 季级策略能达到日级策略的统计置信度——本策略的验证结论天然低功效，仓位设计必须反映这一点。

### §0.4 状态档

```text
RESEARCH → SHADOW（≥2个完整财报季）→ CANARY → LIVE
```

未依次通过 §12 门禁不得跳级。§11.5 样本量不足时，策略永久停留 `RESEARCH`，这是可接受结局。

---

## §1 经济闭环与可证伪命题

### §1.1 三个独立命题

| 编号 | 命题 | 检验方式 | 失败后果 |
|---|---|---|---|
| H-R | M_C 确认事件对"题材后续基本面产业化"（§11.3 算法标签）的 precision 显著高于无条件基率 | 全题材宇宙面板，非手工案例 | 交接测量失败，停止策略 |
| H-A | 确认后按 §8 入场的组合，26/52 周净超额相对趋势基线 A0 与动量匹配对照均 >0（置信下界） | §11.4 事件研究＋组合模拟 | 无增量 Alpha，降级为普通趋势策略并弃用 SFL-2 名称 |
| H-X | E1–E5 退出后标的 26 周超额 ≤0（退出正确率），且带退出规则的组合回撤显著小于确认后买入持有 | §11.6 | 退出引擎无效，仅保留 E5 兜底线 |

H-R 成立不能替代 H-A：正确识别交接但入场太迟或拥挤，仍可能无利润。

### §1.2 收益定义

每笔持仓同时计算：

\[
r^{net}=CashIn/CashOut-1
\]

\[
r^{alpha}=r^{net}-r^{CSI300}\ (\text{同持有区间、虚拟lot法，同 L4-EG v2 §1.3})
\]

成本按 §8.4 全额计入。组合层面以周度净值序列结算。

---

## §2 决策时钟与信息截止

### §2.1 双时钟

| 时钟 | 触发 | 动作 |
|---|---|---|
| 周度时钟 | 每周最后一个交易日收盘后 | 计算全部状态向量、确认判定、排序、生成下周订单 |
| 事件时钟 | 财报/预告/快报披露 | 披露日次一交易日执行 E1 评估；验证事件进入加仓日历 |

订单执行统一在**下一周首个交易日**（事件退出为披露后次一交易日），以开盘后 09:35–10:05 的 TWAP 为基准价，限价参与。

### §2.2 PIT 纪律

1. 所有基本面数据以**交易所披露时间**入库；禁止按报告期日期回填；
2. 分析师一致预期（如使用）以预测发布日为准；
3. 题材成员关系以 §3.2 快照日期为准；快照日期 > t 的成分不得用于 t 日任何计算；
4. 周五收盘后的计算不得使用周五收盘后发生的披露（该披露进入下一周期或事件时钟）；
5. 回测与实盘共用同一特征代码路径，所有记录保存源时间、接收时间、入库时间；
6. **价格口径**：一切收益、均线、回归、CAR、指数计算一律使用后复权收益序列；订单、成交与费用记录使用当时真实未复权价格；
7. **事件成熟日**：任何依赖 CAR(0,+5) 的判定，事件成熟日 = 披露日后第 6 个交易日；未成熟事件不得进入任何信号、验证或加仓判定。

---

## §3 数据契约

### §3.1 必需数据

| 数据域 | 粒度 | 必需字段 | 用途 |
|---|---|---|---|
| 概念板块清单快照 | 每日存档 | 全部板块ID、名称、创建/删除状态 | 题材宇宙存在性（防死板块幸存偏差） |
| 概念板块成员快照 | 每日存档 | 板块ID、成员代码列表、快照抓取时间 | 题材宇宙（核心资产） |
| 日线行情 | 日 | 未复权OHLCV、成交额、换手、自由流通市值 | 执行与成交记录 |
| 复权因子 | 日PIT | 除权除息日、复权因子、发布时间 | 全部收益类计算 |
| 涨停事件 | 日 | 收盘涨停标记、连板数 | B4、S1 判定 |
| 财报/预告/快报 | 事件PIT | 披露时间、营收、扣非净利、同比、预告区间 | B6、L5、E1/E3 |
| 财报分部数据 | 事件PIT | 分部名称、分部收入、披露时间 | L4 正宗性 |
| 公募基金持仓 | 季/半年PIT | 披露日、持股市值 | B5、E4 |
| 龙虎榜 | 事件PIT | 机构专用席位买卖额 | B5 次级层 |
| 指数行情 | 日 | 沪深300 OHLC | 超额与调整日定义 |
| 证券主数据 | 日PIT | ST、停牌、上市日、退市 | 排雷与可交易性 |
| 分析师一致预期（可选） | 日PIT | EPS预测、发布日 | E1/E3 增强层 |

### §3.2 题材成员快照库（v1.0 最高优先级资产）

**问题**：数据商概念成分是活表，事后增删（表现好的股票被后补进成分）。用当前成分回测历史 = 系统性未来函数。

**契约**：

1. 自 v1.0 生效日起，每个交易日收盘后抓取全部概念板块成分并**只追加存档**（SHA-256＋抓取时间）；
2. 历史区段按以下优先级重建，且每条成员关系必须带 `membership_asof_date`：
   - a) 历史成分存档（数据商历史成分表、网页存档），存档日期即快照日期；
   - b) 版本化关键词分类器：仅用披露日 ≤ t 的公告/年报/招股书文本判定成员关系，词典版本化、只追加；
   - c) 两者皆不可得的区段：该题材该区段标记 `MEMBERSHIP_UNVERIFIED`，只能用于 H-R 的定性检查，**不得进入 H-A 收益统计**；
3. 成员进入快照的日期晚于其大涨，是数据的真实属性，不允许人工提前；
4. 每日快照差异（新增/剔除）单独入库，作为"vendor 后视偏差"的量化审计材料；
5. **板块存在性同样 PIT**：历史区段的板块清单优先用当期存档（含后来被删除的死板块）；不可得区段的题材宇宙整体标记 `UNIVERSE_UNVERIFIED`，其上的确认事件不得进入 H-R 基率与 H-A 统计；
6. **词典初版后视披露**：关键词分类器词典、§4.1 排除词典、§7.3 分部词典的初版构建日期、依据与作者写入研究注册表；§11.7 增设词典敏感性测试（随机删除 20% 词条重跑，H-R/H-A 方向不得反转）。"只追加"约束未来，不消除初版以 2026 年事后知识构建的固有偏差——该偏差在最终报告中必须声明。

### §3.3 自建 LOO 题材指数

不使用数据商板块指数（成分变动不可审计）。对任意 t：

\[
I_c(t)=\text{mean}\{r_i(t):i\in msnap(c,t),\ \text{非停牌},\ \text{上市}\ge120\text{日}\}
\]

（\(r_i\) 为后复权日收益。）计算个股纯度时使用剔除自身的 \(I_{c,-i}\)。**成员有效数 <10 时该题材当日全部信号 NA**（与 §4.1 宇宙门槛统一，消除 8/9/10 三处不一致）。

### §3.4 数据质量门 Gate-D

任一触发，该题材当周不可判定（不改变已有持仓的退出监控）：

- 成员快照缺失或 `MEMBERSHIP_UNVERIFIED`；
- 核心组任一股票行情缺失；
- 财报数据接口校验失败；
- 主备源收盘价不一致 >0.5%。

---

## §4 题材宇宙与状态机

### §4.1 宇宙定义（去手工挑选）

监控宇宙 = 全部满足以下条件的概念板块（每周重算）：

- 有效成员 ≥10 只；
- 板块存在 ≥120 交易日；
- 成员 ADV20 中位数 ≥ P-01（3000 万元）；
- 非纯风格/资金面板块（融资融券、标普概念等，用版本化排除词典）。

**任何题材不得人工加入或移出宇宙**；CPO 等案例只能作为宇宙成员被算法命中。

### §4.2 状态机（全部算法化）

以下均按周度时钟评估，`ZTdays(c,w)` = 板块成员在窗口 w 内的收盘涨停股·日总数：

| 状态 | 判定 |
|---|---|
| S0 潜伏 | 非以下任何状态 |
| S1 情绪 | ZTdays(60日) ≥ P-02（30）或 板块指数20日收益 ≥ P-03（+25%） |
| S3 产业 | 曾触发 S1 ∧ M_C 输出 CONFIRMED（确认即生效，不适用双周一致规则——M_C 的 10 个可判定日持续要求即自带滞回） |
| S4 拥挤 | S3 中 E4 条件成立 |
| S5 衰亡 | 持仓触发 E1/E3，或 M_C REVOKED |

规则说明：

- "曾触发 S1"是 M_C 确认的前置条件（无情绪段前科的板块不存在"交接"，防止把慢牛蓝筹误判为政权交接）；回看窗为 750 个交易日；`MEMBERSHIP_UNVERIFIED` 区段内的 S1 历史不可用，即该前置条件视为不成立；
- v0.1 的 S2（交接段）与 S1C（退潮）取消独立状态：S2 与 S3 的边界就是 CONFIRMED 事件本身，S1C 的功能已由 B4 的"先热后冷"条件承担；
- S0/S1 的切换需连续两个周度评估一致；S5 即时生效；
- **入场与 H-A 事件研究的事件日统一为 M_C 的 CONFIRMED 判定日**，不使用状态标签日。

---

## §5 M_R：六维行为状态向量

全部信号定义为**日频状态变量** \(B_k(c,t)\in\{1,0,NA\}\)，周度时钟读取最新值。混合频率数据通过"最近可用值＋失效期"折算到日频。

### §5.0 核心组定义

\[
Core(c,t)=\text{purity前1/3}\cap\text{自由流通市值前40\%}\cap\text{ADV20前40\%}
\]

不足 3 只时按 purity×市值联合排名取前 3。核心组等权。purity 定义见 B1；**purity 不可得的成员（上市不足 120 日或有效样本 <100）不进入三分组，也不进入 Core**。

**时变成分的序列语义（统一规定）**：Core、PX、高/低纯度组均为"逐日按当日快照重算成分、逐日再平衡"的链式收益序列；任何回看统计（\(\max_{250d}\)、\(spread60_{t-20}\) 等）都读取**当时**计算并已入库的历史值，禁止用 t 日成分回溯重构历史（防"今日选组回看昨日"的机械选择偏差）。所有 \(\max\) 类比较统一取"截至 t−1"的历史极值。

### §5.1 B1 纯度分化

120 日窗口（有效样本 ≥100），对每个成员 i 回归日超额收益：

\[
r_i-r_{300}=\alpha+\beta_i(I_{c,-i}-r_{300})+\varepsilon
\]

\[
purity_i=\max(\beta_i,0)\times R_i^2
\]

按 purity 三分（可分组成员 <10 只则 B1=NA）。高、低纯度组等权 60 日累计收益差记 `spread60`（链式序列，见 §5.0）：

\[
B_1=1\{spread60\ge P\text{-}04\ (10\%)\ \wedge\ spread60_t-spread60_{t-20}>0\}
\]

其中 \(spread60_{t-20}\) 是 t−20 日当时入库的值（当时的分组），不是用今日分组回算。

### §5.2 B2 换手沉淀

核心组等权换手率 20 日均值 `TO20`，核心组等权价格指数 `PX`：

\[
B_2=1\{TO20\le P\text{-}25\times\max_{120d}TO20\ \wedge\ PX\ge P\text{-}26\times\max_{250d}PX\}
\]

（两个 \(\max\) 均取截至 t−1 的链式序列历史值。）

### §5.3 B3 抗跌强度

调整日 = 沪深300 日收益 ≤ −1%。取 t 前 250 个交易日内的全部调整日（个数 <10 则 NA），`ARS(t)` = 核心组等权收益 − 沪深300 收益在这些调整日上的**平均值**（平均口径消除调整日个数变化带来的不可比）：

\[
B_3=1\{ARS(t)>0\ \wedge\ ARS(t)\ge\max_{s\in[t-250,t-1]}ARS(s)\}
\]

### §5.4 B4 梯队毕业

必须先热后冷才叫毕业：

\[
B_4=1\{ZTdays(c,[t-19,t])\le P\text{-}05\ (2)\ \wedge\ ZTdays(c,[t-79,t-20])\ge P\text{-}06\ (10)\ \wedge\ PX_{20日收益}\ge0\}
\]

涨跌幅制度差异（10cm/20cm/30cm 板块）导致 ZTdays 跨板块不可直接比较：P-02/P-05/P-06 按主板口径设先验，创业板/科创板成员的涨停日按 P-24（0.6）权重折算（A 类可校准）；北交所成员不计入 ZTdays。制度变更日（如 2020-08-24 创业板改 20cm）前后按当时实际制度取涨停判定。

### §5.5 B5 买家结构（分层代理，可用性显式化）

| 层 | 数据 | 定义 | 失效期 |
|---|---|---|---|
| L1 | 公募持仓（披露日入库） | 核心组公募持仓市值/自由流通市值，**同口径**最近两期环比上升（季报前十大重仓只与季报比，半年/年报全持仓只与半年/年报比，禁止跨口径） | 披露后 130 自然日 |
| L2 | 龙虎榜机构席位 | 核心组 60 日机构席位净买额 >0（60日内上榜 ≥3 次才可用） | 滚动 |

B5 取可用的最高层；均不可用 → NA。**B5 是最直接但最弱的可得信号，v1.0 明确接受其经常 NA。**

### §5.6 B6 业绩定价（PEAD 方向）

取簇成员截至 t 的最近 180 日内全部**已成熟**业绩事件（正式报告/预告/快报，披露日为准；成熟日 = 披露后第 6 个交易日，见 §2.2；未成熟事件不计入）：

\[
B_6=1\{\#matured\ge3\ \wedge\ median(CAR)>0\ \wedge\ CAR_{latest\ matured}>0\}
\]

成熟事件数 <3 → NA。CAR(0,+5) 为事件日至第 5 个交易日相对沪深300 的后复权累计超额。

---

## §6 M_C：交接确认器

### §6.1 可判定性与法定人数

日度状态向量 \(V(c,t)\)，非 NA 个数 \(n_{av}\)：

- \(n_{av}<4\)：当日**不可判定**（不计入确认计数，也不清零）；
- \(n_{av}=4\)：需 4/4 触发；
- \(n_{av}\ge5\)：需 ≥4 触发。

### §6.2 确认与撤销（滞回）

- **CONFIRMED**：前置条件（曾 S1）成立，且最近 P-07（10）个**可判定**交易日全部满足法定人数；
- **REVOKED**：确认后，最近 P-07 个可判定交易日全部满足"反向法定人数"（触发数 ≤ \(n_{av}-4\)）；
- 确认与撤销之间为保持态；不可判定日冻结计数器；
- 同一题材撤销后重新确认需重新走满流程，且 P-20（90 自然日）内至多确认一次（防振荡）。

### §6.3 确认输出

确认事件记录：题材ID、确认日、成员快照哈希、**确认日 Core(c,t) 名单（冻结，供 H-R 标签与 H-A 事件研究使用）**、六信号轨迹、\(n_{av}\) 历史。这是 H-R/H-A 检验的原子事件。

---

## §7 M_Q：中军资格与排序

### §7.1 六层资格（可复现化）

| 层 | v1.0 定义 | 不可得处理 |
|---|---|---|
| L1 成员 | msnap(c,t) ∩ 快照日期 ≤ t | 硬条件 |
| L2 容量 | 自由流通市值与 ADV20 均簇内前 40% | 硬条件 |
| L3 纯度 | purity ≥ 簇内中位 ∧ 与 \(I_{c,-i}\) 的 120 日相关 ≥0.60 | 硬条件 |
| L4 正宗性 | 版本化分部映射词典：题材相关分部收入/总收入，簇内分位 | 分部不可得→取簇内中位分位并标记 `DEGRADED`，该股仓位上限减半 |
| L5 业绩 | 最近披露期营收或扣非净利同比 >0 且不低于上期同比 | 硬条件（无报表无资格） |
| L6 排雷 | 非ST、上市 ≥250 日、无进行中立案调查、大股东质押比例 <50% | 硬条件 |

### §7.2 排序

**放弃 v0.1 的 0.30/0.30/0.25/0.15 权重**（样本不足以校准四位小数的伪精度），同时**删去"机构化趋势"分量**（B5 是 {1,0,NA} 状态变量，无法产生簇内分位，且经常 NA——保留它会引入未定义行为）。

综合分 = 纯度、业绩增速、正宗性三个簇内分位的**等权平均**，权重冻结不许调。业绩增速口径：TTM 扣非净利同比；TTM 不足四期时用最近披露期营收同比替代并标记。每题材取综合分前 P-08（4）只，按排名 4:3:2:1 梯度分配题材内仓位；DEGRADED 股票被减半的额度顺延给下一排名的合格股票，无合格者则留现金。

### §7.3 分部映射词典

`segment_dictionary_<version>.csv`：分部名称关键词 → 题材ID。只追加、版本化；人工修订只进下一版本，禁止回写历史回测。

---

## §8 建仓与执行

### §8.1 三批建仓

| 批次 | 触发 | 仓位 |
|---|---|---|
| 首仓 | M_C 确认后的下一周首个交易日 | 计划的 1/3 |
| 加仓一 | 首次**季报验证事件成熟**后的下一周度时钟对应的执行日 | 1/3 |
| 加仓二 | 第二次验证事件成熟，或周线回踩收复事件（周收盘跌破20周均线后又收回，且未触发任何 E 信号） | 1/3 |

**季报验证事件**定义（防自我叙事）：披露期营收或扣非净利同比增速 ≥ 上一披露期，**且**事件 CAR(0,+5)>0——报表好且市场认账才算验证。**CAR 判定只能在事件成熟日（披露后第 6 个交易日）之后进行**，加仓触发时点以成熟日为锚，消除"下一周一时 CAR 窗口未走完"的未来函数。

### §8.2 执行纪律

- 基准价：执行日 09:35–10:05 TWAP，限价 = 基准价 ×(1+0.5%)；
- 单日单股成交 ≤ 该股当日成交额预估的 2%（用 ADV20 预估）；未完成部分顺延下一交易日，最多顺延 5 个交易日后放弃；
- 执行日 09:35 时（最新价/昨收−1）>8% 则该批次顺延至下一周度时钟重估（不追高纪律的可执行化）；
- 一字涨停/停牌不可成交按实际记录，禁止理论价回填。

### §8.3 卖出执行

- 硬退出（E1/E2/E5/E6）：触发后次一交易日起，每日以开盘价 ×(1−1%) 限价卖出，直至清仓；跌停无买盘按 L4-EG v2 §12.3 的持续申报规则处理并记录实际尾损；
- 软退出（E3/E4）：两批，各 1/2，间隔 ≥5 个交易日。

### §8.4 成本模型

佣金万 2.5（最低 5 元）、印花税卖出千 0.5（按历史费率表）、冲击成本按参与率固定加 15bp/边；敏感性测试 ×2。周频大市值策略成本占比低，但必须全额入账。

---

## §9 持有管理与离场

### §9.1 六类退出（公式化）

| 优先级 | 信号 | v1.0 定义 | 执行 |
|---|---|---|---|
| E1 业绩证伪 | 正式报告/快报的扣非净利同比 <0；或归母净利低于公司最近一次预告区间下限（预告口径为归母，比较用归母，禁止跨口径）；或（有一致预期时）归母净利低于一致预期 10% 以上 | 硬退出，全仓 |
| E2 政权回退 | M_C REVOKED | 硬退出，全仓 |
| E3 增速拐点 | TTM 扣非净利同比增速连续两个披露期环比下降（\(\Delta g_t<0\wedge\Delta g_{t-1}<0\)） | 软退出，两批 |
| E4 拥挤钝化 | 主项：披露期营收与扣非双双创4期最高同比增速，但成熟事件 CAR(0,+5)≤0（利好钝化）；辅项（可得时）：公募持仓占比 ≥ 该股5年 90% 分位 | 软退出，两批 |
| E5 趋势破位 | **个股**后复权周收盘 < 该股30周SMA 连续 2 周 | 硬退出，全仓 |
| E6 深度回撤兜底 | 个股相对持仓期最高周收盘回撤 >30% 且 E1–E5 均未触发 | 硬退出，全仓（视同 E5） |

### §9.2 裁决与回撤容忍的从属关系（修复 v0.1 冲突）

优先级：**E1=E2=E5=E6（硬）> E3=E4（软）> 回撤容忍**。

- "产业段容忍 ≤30% 回撤"（相对持仓期最高周收盘价）仅在 E1–E6 均未触发时适用；超过 30% 即触发 E6，"容忍"因此是有界的可执行规则而非态度声明；
- E5/E6 与容忍冲突时硬信号无条件优先——容忍的是趋势内的波动，不是趋势破位；
- 组合级回撤阶梯（§10.3）优先于个股容忍：阶梯触发的减仓按个股综合分逆序执行。

### §9.3 月度复检

每月最后一个周度时钟：全部持仓题材重算六信号；当日可判定（\(n_{av}\ge4\)）且触发数 ≤2，连续两次月检成立 → **复检降级**（非 E2）：该题材新批次加仓冻结，直至月检恢复；当日不可判定则该次月检顺延至下一周度时钟。

---

## §10 组合与风控（一致性重建）

### §10.1 仓位约束

| 约束 | 值 | 编号 |
|---|---|---|
| 总权益仓位 | ≤90% NAV | P-09 |
| 单题材 | ≤30% NAV | P-10 |
| 产业链合并暴露（版本化链映射表） | ≤40% NAV | P-11 |
| 单股 | ≤12% NAV（DEGRADED 股 ≤6%） | P-12 |
| 并行题材数 | ≤3 个 | P-13 |

**修复 v0.1 的 4×30%=120% 矛盾**：题材数上限从 4 降为 3，且受总仓 90% 硬约束；三约束同时生效取最小。

### §10.2 一致性压力算例（设计时验算，非运行逻辑）

最劣情形：3 题材各 30%（=90% NAV）同时回撤 −30% → 组合 −27%。该值超过 −20% 预算，说明**不能等回撤发生再管**，因此设阶梯：

### §10.3 回撤阶梯

组合净值相对高水位：

- −10%：停止新开题材与加仓；回撤收窄至 −7% 以内解除（滞回防振荡）；
- −15%：全部持仓**当前市值**等比降至该时点的 2/3；回撤收窄至 −12% 解除新减仓义务（已减部分不自动回补，回补走正常建仓规则）；
- −20%：当前市值降至 1/2，全部题材状态强制复核，复核未通过者清仓。

阶梯执行后，个股 −30% 容忍自动失效（组合优先）。回撤阶梯与 E 信号叠加时先执行 E 信号。

### §10.4 现金档位

无 CONFIRMED 题材时仓位为 0。**空仓是本策略常态**。注意区分两个口径：**组合层**（通过全部资格与风控、实际建仓的题材）预期每年个位数；**宇宙层**（全部数百板块上的 CONFIRMED 事件，H-R/H-A 的统计单位）预期一个数量级更多——§11.5 的样本门计的是宇宙层。

---

## §11 回测与统计验证（根本重构）

### §11.1 验证宇宙

- 全部满足 §4.1 的概念板块 × 周度面板（预计数百板块 × 2018–2026）；
- **禁止用手工案例列表作统计主体**；CPO/元宇宙等仅作机制叙事与 sanity check；
- `MEMBERSHIP_UNVERIFIED` 区段从 H-A 统计中剔除并单独报告剔除比例（>50% 则判定历史数据不足，策略停留 RESEARCH）。

### §11.2 时间切分

```text
校准段：2018-01 至 2021-12（仅用于确定 P-04~P-07 等信号参数）
验证段：2022-01 至 2024-12（评估 H-R/H-A/H-X；可迭代次数登记）
final holdout：2025-01 至 2026-06（一次性运行）
```

全部研究尝试写只追加注册表。参数在进入验证段前冻结；验证段失败允许回到校准段修改，但**验证段最多迭代 5 次**，每次登记；超出即验证段证据作废，只保留 holdout 的一次性方向检验。最终报告以 Deflated Sharpe Ratio（Bailey–López de Prado）按登记尝试次数校正显著性。

**holdout 的证据权重声明**：holdout（2025-01–2026-06）早于本 SPEC 生效日，其成员数据同样来自 §3.2 重建路径，并不比验证段更干净；最终报告必须单独披露 holdout 内 `UNVERIFIED` 与词典重建占比，holdout 只承担"方向一致、无灾难"的复核职能，不作为独立统计证据。

### §11.3 H-R：确认器质量

- 产业化标签（仅用于事后评估，不进入信号）：确认日后 52 周，**确认日冻结的 Core(c, t_confirm) 名单**的 TTM 扣非净利同比中位数 ≥ +30% 且题材指数超额 >0 → 记"真产业化"（名单冻结防止用 52 周后的核心组构成引入幸存选择）；
- 基率分母固定为：同期满足确认前置条件（曾 S1、可判定、非 UNVERIFIED）的题材-周；
- 报告 precision、每年确认次数、相对基率的提升；
- 已知伪案例（元宇宙 2021、区块链 2019）必须输出不确认或确认后被 E1/E2 快速退出——作 sanity check。

### §11.4 H-A：增量收益（核心经济检验）

对照组（同期、同宇宙）：

1. **A0 趋势基线**：题材指数（自建 LOO 全簇指数）站上其30周SMA即买入同规则核心组，题材指数跌破30周SMA连续2周卖出；A0 受与本策略完全相同的仓位约束、回撤阶梯与成本模型——检验 M_R/M_C 是否优于朴素趋势；
2. **动量匹配对照**：每个确认事件，从**截至该事件日**未处于 CONFIRMED 状态且不在冷却期的题材中，选确认日前 26 周收益最接近的 5 个构建对照组（"未确认"以事件日实时状态判定，禁止用全样本期事后信息排除后来确认的题材）；对照题材在持有窗口内若后续被确认，保留在对照组并在报告中披露该比例；
3. 沪深300。

统计：

- 事件级 26/52 周 \(r^{alpha}\)，重叠窗口用按月 block bootstrap（块长 3 个月）＋题材聚类；
- 组合级：全规则周频模拟（含成本、阶梯、退出）净值 vs A0 净值；
- 通过标准：事件级与组合级超额的 90% 置信下界 >0（低频策略用 90% 而非 95%，功效诚实声明，且以仓位上限补偿）。

### §11.5 最低样本门

验证段**宇宙层** CONFIRMED 事件 ≥ P-14（25）个，否则输出 `INSUFFICIENT_SAMPLE`：策略停留 RESEARCH，可继续影子运行积累样本，**不得因样本不足而放宽确认阈值**。设计上已知的张力必须在功效核算中显式讨论：\(n_{av}=4\) 需 4/4、B5/B3/B6 易 NA，会压低确认产出——若因此样本不足，正确结论是"该机制以当前数据不可验证"，而不是放松法定人数。

### §11.6 H-X：退出质量

- 每类 E 信号触发后标的 26 周超额分布（应中位 ≤0）；
- 带退出 vs 确认后买入持有 52 周：回撤对比、收益对比；
- E5 单独检验（它是唯一无需基本面数据的兜底）。

### §11.7 稳健性

全部稳健性测试**只在校准段＋验证段执行**，不触碰 holdout：

- 法定人数（3/4/5）、持续日数（5/10/15）、B1 阈值（5%/10%/15%）平移测试，结论方向不得反转；
- 剔除 AI 算力链（验证段内）后重跑（单一产业周期依赖检查）；
- 词典敏感性：随机删除 20% 词条重跑（种子固定），H-R/H-A 方向不得反转；
- 子期（2022/2023/2024）分别报告。

---

## §12 验证门

| 门 | 内容 | 通过标准 |
|---|---|---|
| G0 复现 | 独立环境从快照重跑 | 状态向量、确认事件、订单逐条一致 |
| G1 PIT | 抽查 ≥100 条成员关系与财务字段 | 无快照日期倒挂、无披露日回填 |
| G2 信号健康 | 六信号可用率、NA 分布、分年稳定性 | B5 除外的信号可用率 ≥80% |
| G3 H-R | §11.3 | precision ≥ 2× 基率且伪案例 sanity 通过 |
| G4 H-A | §11.4 | 事件级与组合级 90% 下界 >0，两类对照均通过 |
| G5 H-X | §11.6 | E1/E2/E5/E6 退出后超额中位 ≤0；组合回撤改善 |
| G6 稳健 | §11.7 | 方向不反转；剔除单一产业链后 G4 仍同号 |
| G7 影子盘 | ≥2 个完整财报季 | 信号与离线一致；执行偏差 ≤ 成本模型假设 |

G3–G6 须在 final holdout 上以同一冻结版本复核（样本极少，holdout 只验方向与无灾难，不重复置信区间要求）。

---

## §13 参数登记表

参数分两类。**A 类（可校准）**：只允许在 §11.2 校准段调整，进入验证段前冻结。**B 类（冻结设计常数）**：风控与统计协议常数，任何阶段不得当作优化变量（防止把风险偏好回测成"最优参数"）。

**A 类（可校准）**

| 编号 | 参数 | 先验值 | 用途 |
|---|---|---:|---|
| P-02 | S1 涨停天次阈值 | 60日≥30 | 情绪段判定 |
| P-03 | S1 指数收益阈值 | 20日+25% | 情绪段判定 |
| P-04 | B1 纯度分化阈值 | 60日超额10% | 信号 |
| P-05 | B4 近期涨停上限 | 20日≤2 | 信号 |
| P-06 | B4 前期涨停下限 | 前60日≥10 | 信号 |
| P-07 | 确认持续日数 | 10个可判定日 | 确认/撤销 |
| P-15 | E1 一致预期miss阈值 | 10% | 退出 |
| P-16 | E4 公募持仓分位 | 5年90% | 退出辅项 |
| P-24 | 20cm板块涨停折算权重 | 0.6 | ZTdays 跨制度可比 |
| P-25 | B2 换手压缩比 | 0.70 | 信号 |
| P-26 | B2 价格强度比 | 0.90 | 信号 |

**B 类（冻结设计常数）**

| 编号 | 参数 | 值 | 用途 |
|---|---|---:|---|
| P-01 | 宇宙 ADV20 中位下限 | 3000万元 | 宇宙过滤 |
| P-08 | 每题材持仓数 | 4只 | 组合 |
| P-09 | 总仓上限 | 90% NAV | 风控 |
| P-10 | 单题材上限 | 30% NAV | 风控 |
| P-11 | 产业链合并上限 | 40% NAV | 风控 |
| P-12 | 单股上限 | 12%（DEGRADED 6%） | 风控 |
| P-13 | 并行题材上限 | 3个 | 风控 |
| P-14 | 验证段最低宇宙层确认数 | 25 | 样本门 |
| P-17 | 回撤阶梯（含滞回） | −10/−15/−20%；解除 −7/−12% | 风控 |
| P-18 | 个股回撤容忍→E6 | −30%（持仓期最高周收盘） | 退出兜底 |
| P-19 | E5 破位线 | 个股30周SMA×2周 | 退出 |
| P-20 | 再确认冷却期 | 90自然日 | 防振荡 |
| P-21 | 执行限价偏离 | 0.5% | 执行 |
| P-22 | 单日参与率 | 2%×ADV20 | 执行 |
| P-23 | 冲击成本 | 15bp/边 | 成本 |
| P-27 | 题材成员有效数下限 | 10只 | 信号可判定 |
| P-28 | purity 回归窗口/最低样本 | 120日/100日 | 信号 |
| P-29 | B3 调整日定义与最低数 | 沪深300≤−1%；250日内≥10个 | 信号 |
| P-30 | B6 事件窗与最低数 | 180日内成熟事件≥3 | 信号 |
| P-31 | Core 分位 | purity前1/3×市值前40%×ADV20前40% | 遴选 |
| P-32 | L3 相关性下限 | 0.60 | 遴选 |
| P-33 | L6 质押上限 | 50% | 排雷 |
| P-34 | S1 前科回看窗 | 750交易日 | 状态机 |
| P-35 | 不追高线 | 09:35涨幅>8% | 执行 |
| P-36 | 加仓二回踩线 | 20周SMA | 建仓 |
| P-37 | 验证段最大迭代次数 | 5 | 研究纪律 |
| P-38 | 月检降级条件 | n_av≥4 且触发≤2，连续2次 | 持有管理 |

---

## §14 失效模式

| 失效模式 | 识别 | 处理 |
|---|---|---|
| 成员快照污染（vendor 后补） | §3.2 快照差异审计异常 | 相关区段降级 UNVERIFIED，重跑受影响统计 |
| 确认器退化为动量代理 | 动量匹配对照无增量 | H-A 失败，策略降级弃名 |
| B5 长期不可用 | 可用率监控 | 按 §6.1 自适应法定人数运行，不硬造数据 |
| 财报造假/追溯重述 | 立案公告、审计意见 | 触发 E1 同级硬退出；重述数据不回改历史信号 |
| 产业周期样本枯竭 | 验证段确认数 <P-14 | INSUFFICIENT_SAMPLE，停留 RESEARCH |
| 单一产业链依赖 | §11.7 剔除测试失败 | 判定过拟合于 AI 周期，重新校准 |
| 制度变化（涨跌幅/披露规则） | 规则主数据 | 相关信号（B4 等）版本切换并重验 |
| 三策略同标的冲突 | 合并集中度报表 | 净额合并计 P-12，纪律各自独立，慢钟优先 |

---

## §15 最小复现包

```text
config/
  parameters_v1.yaml
  universe_exclusion_dictionary_<ver>.csv
  segment_dictionary_<ver>.csv
  chain_map_<ver>.csv
data_manifest/
  membership_snapshots_manifest.json
  fundamental_pit_manifest.json
signals/
  state_vectors_<run_id>.parquet
  confirmations_<run_id>.parquet
reports/
  hr_ha_hx_<run_id>.html
  portfolio_sim_<run_id>.parquet
audit/
  pit_samples_<run_id>.csv
  vendor_bias_audit_<run_id>.csv
  research_registry.jsonl（只追加）
```

复现输入仅接受 `data_snapshot_id + code_commit + config_version + run_id`；随机过程固定种子并写入报告。

---

## 附录 A　每周运行手册

```text
【每周最后一个交易日收盘后】
 □ 抓取并存档全部板块成员快照（哈希入库）
 □ 更新行情/涨停/财报 PIT 库，Gate-D 自检
 □ 重算宇宙、状态机、六维状态向量
 □ M_C 确认/撤销判定；输出确认事件与撤销事件
 □ 持仓题材月度复检（每月最后一周）
 □ 生成下周首个交易日订单（新仓/加仓/软退出批次）
 □ 全部输出带 decision_id 与理由码

【每个披露日次一交易日】
 □ E1 评估：触发即生成硬退出订单
 □ 验证事件登记：进入加仓日历

【每周首个交易日】
 □ 09:35–10:05 TWAP 限价执行；记录成交/未成交
 □ 更新组合净值与回撤阶梯状态
```

## 附录 B　v0.1 问题 → 根因（RCA）→ v1.0 修复映射

| # | v0.1 问题 | 根因 | v1.0 修复 |
|---|---|---|---|
| 1 | 概念成分用现值回测 | 数据商成分是事后维护的活表，框架未把"成员关系"当作带时间戳的数据 | §3.2 快照库＋membership_asof＋UNVERIFIED 降级＋vendor 偏差审计 |
| 2 | 手工案例库当验证 | 用"记得住的大牛股"替代总体宇宙——幸存者与后视选择 | §11.1 全宇宙面板；案例仅 sanity check |
| 3 | 14 参数 vs ~10 案例 | 参数自由度与事件稀缺天然冲突，v0.1 未做功效核算 | 参数瘦身＋等权冻结评分＋P-14 样本门＋允许 INSUFFICIENT 结局 |
| 4 | "≥4/6 持续 10 日"无定义 | 六信号频率异构（日/月/季/事件），无统一时钟 | §5 全部折算日频状态变量＋失效期＋§6.1 NA 与可判定性显式化 |
| 5 | B5 依赖不可得数据 | 北向停披、公募季频滞后、龙虎榜覆盖偏差，v0.1 当作常规信号 | §5.5 分层代理＋自适应法定人数（B5 缺席不瘫痪确认器） |
| 6 | 确认≠盈利（缺 Alpha 桥） | 把"识别正确"与"入场后有净超额"混为一谈（与 L4-EG v1 同病） | §1.1 H-A 独立命题＋A0 趋势基线＋动量匹配对照＋成本后组合模拟 |
| 7 | 风控数学矛盾（120% NAV；−30%容忍 vs −20%预算 vs E5） | 各约束局部设计，从未做联合压力验算 | §10 总仓90%＋题材数3＋压力算例＋回撤阶梯＋§9.2 从属关系裁决 |
| 8 | "主升段前20%"等验证标准后视 | 用事后才能定义的主升段给实时信号打分 | §11.3 改为算法化基本面标签＋precision；主升段叙事仅作展示 |
| 9 | E3"EPS二阶导"、"指引下修"不可算 | 照搬美股概念；A 股无常规指引，季度 EPS 噪声大 | §9.1 改用 TTM 扣非同比的披露期环比＋预告区间＋一致预期分层 |
| 10 | 综合分权重 0.30/0.30/0.25/0.15 伪精度 | 无校准可能的自由参数伪装成设计 | §7.2 等权分位冻结 |
| 11 | 板块指数（BK系列）直接入模 | 指数成分变动不可审计 | §3.3 自建 LOO 等权指数 |
| 12 | "首仓=确认日收盘"不可执行 | 未定义执行时点、价格与参与率 | §8 周首日 TWAP＋限价＋参与率＋顺延与放弃规则 |
| 13 | 正宗性"人工复核"不可复现 | 主观判断进入信号链 | §7.3 版本化分部词典＋DEGRADED 显式降级（仓位减半＋额度顺延规则） |
| 14 | 未先热后冷即"毕业" | B4 只看"现在没涨停"，漏掉"从未点火"板块 | §5.4 增加前 60 日涨停天次下限 |
| 15 | 未复权价计算收益信号 | 数据契约只列未复权行情，除权跳空污染全部均线/回归/CAR | §2.2 双口径：信号用后复权，成交记录用未复权；契约加复权因子 |
| 16 | CAR(0,+5) 未成熟即用于触发 | 事件效应窗口与决策时点未对齐 | §2.2 事件成熟日=披露后第6交易日；B6/E4/加仓全部只用成熟事件 |
| 17 | 板块清单与三词典的初版后视 | 只对成员关系做了 PIT，板块存在性与词典内容仍以 2026 年知识构建 | §3.2 板块清单快照＋UNIVERSE_UNVERIFIED＋词典初版登记＋词典敏感性测试＋固有偏差声明 |
| 18 | 动量对照以"从未确认"构造 | 用全样本期未来信息挑对照组，系统性虚增超额 | §11.4 对照以事件日实时状态判定，后续确认者保留并披露 |
| 19 | "机构化趋势"分量无定义 | 把 {1,0,NA} 状态变量当连续因子用 | §7.2 删除该分量，改三因子等权 |
| 20 | −30% 容忍无动作语义 | "容忍"是态度不是规则，突破后无接管条件 | §9.1 新增 E6 深度回撤兜底（视同硬退出）＋§9.2 从属关系 |

---

*本文档由 SFL-2 v0.1 经批判性审查与根因分析重构。策略当前状态 RESEARCH；通过 §12 全部门禁前不得实仓。不构成投资建议。*
