# SFL-2 v4.0 SPEC 的 Critical Review 与根因分析（RCA）
## ——v4.0 → v5.0 修复依据文档

> 审查对象：《SFL-2 v4.0 产业时钟·中军趋势策略 SPEC（可回测版）》及其配套文档《SFL-2 v3.0 SPEC 的 Critical Review 与根因分析（RCA）》
> 审查日期：2026-08-26
> 审查方式：对 v4.0 全文做规则联合可达性推演（重点：事件本体、状态机转移完备性、验证协议的时间一致性）；对 v3.0 审查报告的关键数学断言做独立复核。
> 结论产物：《SFL-2 v5.0 产业时钟·中军策略 SPEC（可回测版）》（同仓库）
> 问题编号前缀 V4- 以区别于 v3.0 审查的 F/L/S/P 编号。

---

## 1 总体评估

### 1.1 对 v3.0 审查报告本身的复核

v3.0 审查报告的核心断言经独立复核**基本成立**：

- `purity_exp = max(ρ,0)³·σ_i/σ_I` 的代数展开正确（β=ρσ_i/σ_I，R²=ρ²，相乘即得）；
- B3 机械分量估算正确（β∈[1.2,1.5]、调整日均值 −1.7% 时为 −34~−85bp/日）；
- ZTD 的 N=40 锚定换算（0.75/0.05/0.25 ↔ 30/2/10 天次）算术正确；
- 12 项 F 级缺陷（状态互斥、DEFCON 死锁、Gate-D 停牌互斥、订单仲裁、holdout、H-R 标签污染、事件去重、相关选票、B3 β 矛盾、数据前置、迭代预算、容量接纳）均为真实缺陷，非吹毛求疵；
- "五个系统性根因"的归纳（逐点修复无联合验算、日历重复≠独立证据、测量与收益命题混同、按沿用而非穷举组织规格、数据可得性未门禁化）准确，且可预测地指向 v4.0 自身的残留缺陷（见下）。

### 1.2 对 v4.0 的判定

v4.0 的结构性建设（状态优先级公理、订单仲裁表、冻结注册表、批次状态机、分组法定人数、G-DATA 前置门、参数穷举至 P-80、预注册固定分析）**方向与执行大体合格**，是四个版本中第一份接近可实施的规格。但联合可达性推演发现 **3 项致命（V4-F）＋12 项逻辑（V4-L）＋3 项统计（V4-S）＋3 项实务（V4-P）残留缺陷**，其中两项恰好由 v4.0 自己的修复动作引入（F7 修复引入 V4-F1；F9 修复的补丁引入 V4-F3）——再次验证"补丁未按主规则同一标准复检"这一系统性根因。

**v4.0 不得作为回测实施依据；v5.0 修复后方可按门禁推进。**

### 1.3 策略思想（维持前两轮结论）

策略思想作为待验证假说成立（PEAD、基本面动量、动量生命周期、A 股涨停情绪周期与资金结构二元性），v4.0 §0.3 把真实身份进一步坦白为"强势整理＋机构赞助＋业绩验证的趋势跟随复合过滤器"并以分组法定人数固化，是诚实的进步。本轮不再重复论证。

---

## 2 致命缺陷（V4-F 级，3 项）

### V4-F1　事件去重规则与事件时钟、验证事件、E1 评估三处直接矛盾——单层事件本体无法同时服务统计计数与决策评估

**问题**：v4.0 §2.2-3 规定事件键（股票, 报告期）**仅最早披露计为事件**，且明文写为"进入 B6 计数、验证日历、**E1 评估的信息起点**"，后续同键披露"仅用于修正数值，**不产生新事件**"。与此同时：

- §2.1 事件时钟规定"财报/预告/预告修正/快报披露 → 次一交易日执行 E1 评估"（每次披露都评估）；
- §8.1 规定"未通过验证条件的披露只登记、不消耗批次"（暗示逐次披露逐次评估）；
- §8.1【P03】注明"预告无营收字段，**验证事件实际只能来自快报/正式报告**"。

三者与去重规则联立后产生不可调和的矛盾：**对发生过业绩预告的报告期（A 股强制预告情形大量存在），快报/正式报告"不是事件"——验证事件永不成立，加仓一/加仓二永不触发，策略对这类公司永远只有 1/3 首仓**。同理，E1 在预告日（无扣非/营收精确值）无法执行完整评估，而正式报告到达时"不产生新事件"，E1 是否重评无定义。

**RCA**：F7 的修复目标是"B6 统计计数膨胀"，但修复动作把去重升格为**全局事件本体论**，覆盖了本应逐次评估的决策链路。统计层（计数不能重复）与决策层（信息更新必须重评）对"事件"的需求本质不同，单一定义无法同时满足——这是"补丁未做全链路影响分析"的又一实例。

**v5.0 修复（§2.2-3 重写为双层事件模型）**：

- **信息事件（决策层）**：每次披露（预告/预告修正/快报/正式报告）均为信息事件，次一交易日触发 E1 重评（以截至当日的最新数值，按证伪轨道可评估的分支评估）与验证条件评估（仅当该披露含所需字段）；同一报告期可多次评估，但**验证"通过"每报告期至多记一次**（首个通过的披露），批次至多消耗一次；
- **统计事件（计数层）**：B6/S4 的事件计数与 CAR 用事件键去重，锚 = 最早披露，成熟日与 180 日窗以锚计，"≥2 个不同成员"保留；
- **验证事件的 CAR(0,+5)** 以"该次通过验证的披露"自身的事件日起算（其为增量信息的市场反应；若预告已提前消化利好，该口径偏保守，方向可接受并声明）。

### V4-F2　holdout 有效性前提是成文时已知恒假的死代码——"final holdout"名义地位应予废除

**问题**：v4.0 §11.2 的 F5 修复以条件句书写："注册表须存在时间戳 ≤2024-12-31 的冻结记录，否则 holdout 降级为第二验证段"。但本项目的研究注册表 2026-08 才建立，**该前提在成文当日即已知不可能满足**——条件句是永假分支，而全文（§11.2 切分图、§12 复核声明、多处"holdout"措辞）仍维持 holdout 的名义框架。规格中保留已知不可达的分支，等于给未来的报告写作留下"措辞漂移"空间（先写 holdout，被质疑再降级）。

**RCA**：F5 修复选择了"条件豁免"的最小改动路径，回避了对已知事实的直接裁决。规格的职责是把已知事实固化为结构，而不是留给条件句。

**v5.0 修复（§11.2 重构时间切分）**：

```text
校准段：  2018-01 至 2021-12（A 类参数）
验证段A： 2022-01 至 2024-12（H-R/H-A/H-X 主检验；P-37 迭代预算主要消耗于此）
验证段B： 2025-01 至 2026-06（一次性运行的第二验证段；明文为历史数据、semi-out-of-sample）
真前瞻：  SHADOW 影子盘（2026-09 起 ≥2 个完整财报季）——这是本策略唯一可能的 holdout
```

验证段B 在验证段A 完成并冻结参数后**一次性运行**；若失败允许回炉，但必须声明验证段B 已被消费，此后前瞻证据只能来自 SHADOW。DSR 尝试计数覆盖两个验证段。全文废除 "holdout" 措辞。跨段依赖（验证段A 末 52 周事件的标签消耗验证段B 数据）照 v4.0 S11 条款保留并改写指向。

### V4-F3　B3 备用压力日口径重犯 F9 刚修复的机械 β 偏差——备用口径是结构性死信号

**问题**：v4.0 §5.3 主口径已把 ARS 改为对中证1000 的 β 对冲（F9 修复正确）。但备用压力日口径（调整日 <5 时启用，压力日 = **LOO 题材指数** ≤−2%）沿用同一对冲式 \(r_{Core}-\hat\beta_{Core}\cdot r_{1000}\)。在"题材大跌但市场未大跌"的备用压力日上，\(r_{1000}\approx 0\)、对冲项失效，而 Core 对**题材**的 β>1（Core 本就按高 purity_exp 选出），\(r_{Core}\) 系统性深负 → 备用口径下 ARS 结构性为负，B3 永不触发。**为救 B3 可用率而设的备用口径，恰好是一个不可能触发的死信号**——与 F9 被修复前的病理完全同构。

**RCA**：备用口径是 P09（低波动市 NA）的后加补丁，未按主口径同一标准做机械分量分析——"补丁未按主规则复检"根因的第三次显形。

**v5.0 修复（§5.3）**：备用压力日的 ARS 改为对 **LOO 题材指数**的 β 对冲：

\[
ARS^{backup}(t)=\text{mean}_{\tau}\left[\,r_{Core}(\tau)-\hat\beta^{LOO}_{Core,t-1}\cdot I_c(\tau)\,\right]
\]

（\(\hat\beta^{LOO}\) 用截至 t−1 的 120 日回归。）度量语义变为"题材内部压力日 α"（Core 相对题材整体的抗跌），与主口径"市场压力日 α"并列。**两口径的 ARS 分别入库为独立序列，Q80 分位各对本口径的变动日观测计算，禁止混池**；当期生效口径与切换日入 decision_id。

---

## 3 逻辑缺陷（V4-L 级，12 项）

| # | 缺陷 | RCA | v5.0 修复 |
|---|---|---|---|
| V4-L1 | S4 既在互斥优先级链（S5>S4>S3>…）中，又被声明为"S3 的修饰符、并存而非覆盖"——一个对象不能同时是互斥状态和并存标志 | F1 修复引入优先级公理时，未把 L01 的"修饰符"决定同步回状态集定义 | S4 从状态集移除：状态集 = {S0, S1, S3, S5}，优先级 **S5 > S3 > S1 > S0**；"拥挤"仅以 FREEZE_S4 冻结位存在（判定式、2 周滞回、CONFIRMED_CROWDED 规则全部不变）（§4.2） |
| V4-L2 | S5 是吸收态：REVOKED 后无任何离开 S5 的转移，与"撤销后可重新确认"（§6.2）矛盾——重新 CONFIRMED 时状态归属无解 | 状态机只写了进入条件，未写退出转移 | S5 退出转移：a) 重新触发 S1 判定式 → 转 S1（前科重记）；b) 重新 CONFIRMED → 转 S3。二者先到先转（§4.2） |
| V4-L3 | "加仓日历自首仓**完全成交日**起算"——首仓因参与率顺延 5 日后部分放弃时，"完全成交日"永不存在，加仓日历永不起算 | 批次状态机假设执行必然完成，未与 §8.2 的放弃规则联立 | 起算点改为**首仓执行终结日** = min(计划额度全部成交日, 顺延放弃日)（§8.1） |
| V4-L4 | A0 对照受容量接纳规则约束，但"LOO 站上 13 周 SMA"在牛市同周大量题材同时满足——同日确认的接纳次序未定义，A0 组合不可复现（违反 G0 精神） | 接纳规则只为策略侧（确认事件天然稀疏）设计，未随 A0 的高触发频率复检 | A0 同周多题材上穿时的接纳序：按题材成员 ADV20 合计降序，再按 board_id 字典序；规则预注册（§11.4） |
| V4-L5 | H-R 基率的"伪事件按相同抽样过程（含 P-20 冷却模拟）抽取"无确定性协议（数量、种子、约束顺序未定义）——基率本身不可复现 | 统计协议写了意图没写算法 | 确定性贪心 thinning：对每题材自最早合格题材-周起，以 P-20 间隔顺序选取伪事件，无随机性；如需子抽样，协议与种子入注册表（§11.3） |
| V4-L6 | 三批加仓各为"重算后计划额度的 1/3"，NAV 与额度在批次间变动时累计持仓可超 P-12 硬顶，执行时点无复核 | 额度公式是计划层约束，缺执行层不变量 | 任何买入执行前检查：执行后单股市值 ≤ P-12×当前 NAV（DEGRADED 6%），超出部分削减留现金（§8.2） |
| V4-L7 | DEFCON1 每轮清仓后高水位重置——跨轮累计亏损无界（每轮最多 −20%，可无限重复），无策略级终止线 | 复位语义修复了死锁，未审视重置的跨轮累积后果 | 新增 **P-81 策略终止线**：净值相对**策略起始值**累计回撤 ≥−35% → 策略终止，回到 RESEARCH 全面复盘，不得以原参数重启（§10.2） |
| V4-L8 | 订单仲裁条款 2"买入顺延至软退出完成后重估（若届时仍持有）"是恒假死文本——软退出目标即 100% 清仓，完成时必然不再持有；且批次是否消耗未定义 | 仲裁表逐行书写，未对每行做可达性检查 | 改为与硬退出同构：买入作废登记理由码、批次不消耗（§2.1、附录 B.2） |
| V4-L9 | L3 的"60 日相关 ≥0.60"未与 purity 估计窗（跳过最近 5 日，[t−65,t−6]）对齐——同一资格层内两个统计量窗口不一致 | S03 修复只改了 §5.1，未同步 §7.1 引用处 | L3 相关性与 purity 同窗 [t−65, t−6]（§7.1） |
| V4-L10 | §1.1 H-X 与 G5 把 E1/E2 也纳入"伪触发对照"，但 §11.6 的伪触发时点构造（同幅度回撤未破位）只对价格型退出（E5/E6）有意义——基本面/确认器型退出无对应伪触发定义 | H-X 重构时命题行与协议行未逐字对齐 | 判据分型：E5/E6 用伪触发对照（90% 上界 <0）；E1/E3/E4 用"触发后 26 周超额中位 ≤0"（分轨道、分预告层）；E2 归入确认器质量随 H-R/G3 评估；§1.1 与 G5 同步改写（§11.6、§12） |
| V4-L11 | B4"后冷"上限（max(1,⌊0.05N̄⌋) 股·日）与 P-24=3.0 折算权重交互：单个 20cm 涨停计 3.0 单位即超上限——近端安静条件对单一偶发事件过敏 | P-24 为跨制度基率对齐设计（密度型条件），被无差别应用到计数上限型条件 | "后冷"上限按**未折算原始涨停股·日**计数（其语义是"近端安静"，与跨制度基率无关）；折算权重仅用于密度型条件（S1、B4 前热）（§5.4） |
| V4-L12 | E3/验证事件的累计同比在跨年边界（Q1 YTD 对上年 FY）比较噪声大，规则未声明处理方式 | 口径统一（L15 修复）未讨论年度边界 | 明示：披露期按自然顺序比较、跨年边界不作特殊处理；报告按披露期类型分层统计；单季口径敏感性保留（§9.1） |

---

## 4 统计缺陷（V4-S 级，3 项）

| # | 缺陷 | RCA | v5.0 修复 |
|---|---|---|---|
| V4-S1 | REVOKED 在低可用度下近乎不可达：n_av=4 时反向法定人数要求 V_eff≤0，任一信号触发即阻断撤销；确认后 B3/B5 转 NA 且 n_av<4 时计数器冻结，题材可无限期滞留 CONFIRMED（E2 失效，退出全靠 E5） | 撤销条件只覆盖"证据反向"，未覆盖"证据消失" | 新增**信息枯竭撤销**通道：确认后连续 P-82（20）个交易日不可判定（n_av<4）→ 自动 REVOKED（定价权证据消失即撤销）；原反向法定人数通道保留（§6.2） |
| V4-S2 | G2 硬门"无 \|ρ\|>0.7 跨族信号对"无处置路径：失败即卡死（信号重设计算什么？校准段已冻结） | 门禁写了标准没写失败协议 | 处置协议：触发时将相关对中的独立族信号并入整理族重算法定人数结构，作为一次登记迭代计入 P-37；重分后独立族可用信号 <2 → 确认器设计失败，输出 `DESIGN_INSUFFICIENT` 停留 RESEARCH（§11.5、§12） |
| V4-S3 | P-04 校准目标"零分化模拟假触发率 ≤10%"的零假设生成协议未定义——不可复现 | 校准目标显式化（S05 修复）只写了目标没写模拟器 | 协议入册：校准段内对每题材成员的 purity 分组标签做**块置换**（块长 20 日，保留指数与波动结构、破坏纯度-收益耦合），置换 100 次、种子登记；P-04 = 使置换样本假触发率 ≤10% 的最小阈值（§13 P-04 provenance、§11.7） |

---

## 5 实务缺陷（V4-P 级，3 项）

| # | 缺陷 | RCA | v5.0 修复 |
|---|---|---|---|
| V4-P1 | 分钟行情历史覆盖（2018 年起的 1/5 分钟 bars）未纳入 G-DATA 前置验证——TWAP 与不追高线的回测可行性同样是数据单点依赖 | G-DATA 只覆盖了成分数据 | G-DATA 验证清单增加分钟行情历史覆盖项；不可得区段的执行模拟降级为"开盘价+固定滑点"并声明（§3.2、§8.2） |
| V4-P2 | 一致预期在校准段（2018–2021）对中小题材股覆盖率低，L5b/E1-miss 分支长期 NA 的程度未被报告义务覆盖 | 报告义务清单未穷举到该分支 | §11.5 报告义务增加：L5 三分支与 E1 各分支的分年可用率（§11.5） |
| V4-P3 | 红利税按持有期档位计扣的现金流时点与实务不符（实际为卖出时倒扣补缴），回测简化未声明 | 成本模型精确化时未标注简化边界 | §8.4 增加声明：回测按持有期档位在分红日计扣为简化处理，与实务"卖出时补扣"存在时点差，方向保守（§8.4） |

另：v4.0 §13 B 类参数表存在 markdown 列数错误（表头 4 列、分隔行 5 列），v5.0 修正——此类排版缺陷会破坏自动解析（parameters.yaml 与 §13 的一致性是 G0 检查项）。

---

## 6 根因归纳（本轮新增证据）

前四版归纳的五个系统性根因在 v4.0 中再次得到验证，本轮补充第六条：

6. **修复动作本身缺乏"与被修复规则同标准"的复检环节** → V4-F1（F7 修复覆盖决策链）、V4-F3（F9 修复的补丁重犯同病）、V4-L1（F1 修复与 L01 修复互斥）、V4-L8（F4 仲裁表含死行）。对策：v5.0 附录 C 的映射表增加"修复影响面"列；任何修复必须列出其触碰的全部下游条款并逐条复检（本轮已按此执行）。

---

## 7 v4.0 问题 → RCA → v5.0 修复映射

| # | v4.0 问题 | 根因 | v5.0 修复 |
|---|---|---|---|
| V4-F1 | 事件去重与事件时钟/验证事件/E1 评估三处矛盾，强制预告公司加仓永不触发 | 统计去重被升格为全局事件本体 | §2.2-3 双层事件模型（信息事件/统计事件） |
| V4-F2 | holdout 前提为已知恒假的死代码 | 条件豁免回避已知事实 | §11.2 重构：验证段A/B＋SHADOW 为唯一前瞻，废除 holdout 措辞 |
| V4-F3 | B3 备用压力日口径机械 β 偏差（结构性死信号） | 补丁未按主规则同标准复检 | §5.3 备用口径改 LOO β 对冲，双口径分池 |
| V4-L1 | S4 状态/标志本体矛盾 | 两处修复未合并 | 状态集 {S0,S1,S3,S5}，S4 仅为冻结位 |
| V4-L2 | S5 吸收态无退出转移 | 只写进入未写退出 | S5→S1/S3 转移规则 |
| V4-L3 | "完全成交日"在放弃情形不存在 | 未与放弃规则联立 | 首仓执行终结日 |
| V4-L4 | A0 容量接纳无 tie-break | 接纳规则未随 A0 复检 | ADV 降序＋board_id 字典序，预注册 |
| V4-L5 | 伪事件抽样无确定性协议 | 写意图未写算法 | 贪心 thinning 确定性协议 |
| V4-L6 | 批次累计可超 P-12 | 缺执行层不变量 | 执行前市值复核 |
| V4-L7 | 连环 DEFCON 累计亏损无界 | 复位语义未审跨轮累积 | P-81 策略终止线 −35% |
| V4-L8 | 软退出×买入仲裁为死文本 | 仲裁表未做行级可达性检查 | 作废不消耗，与硬退出同构 |
| V4-L9 | L3 相关窗未对齐跳 5 日窗 | 修复未同步引用处 | 同窗 [t−65,t−6] |
| V4-L10 | H-X/G5 对 E1/E2 引用伪触发对照无定义 | 命题行与协议行未对齐 | 判据分型（价格型/基本面型/确认器型） |
| V4-L11 | B4 后冷上限×P-24 权重过敏 | 折算权重误用于计数上限 | 后冷用未折算计数 |
| V4-L12 | 累计同比跨年比较未声明 | 口径统一未讨论边界 | 明示自然顺序＋分层报告 |
| V4-S1 | REVOKED 低可用度不可达 | 未覆盖"证据消失" | P-82 信息枯竭撤销 |
| V4-S2 | G2 相关性门无处置路径 | 有标准无协议 | 重分族＋DESIGN_INSUFFICIENT 结局 |
| V4-S3 | P-04 零假设模拟不可复现 | 有目标无模拟器 | 块置换协议入册 |
| V4-P1 | 分钟数据可得性未前置 | G-DATA 清单不全 | 纳入 G-DATA＋降级路径 |
| V4-P2 | 一致预期分支可用率无报告义务 | 报告清单未穷举 | 分年可用率报告 |
| V4-P3 | 红利税时点简化未声明 | 简化边界未标注 | 声明条款 |
| — | §13 B 类表 markdown 列数错误 | 排版 | 修正（G0 依赖） |

---

*本审查同时复核了 v3.0 审查报告的关键断言（§1.1）。v4.0 的结构性框架被 v5.0 完整继承；被修复的是其残留的联合可达性缺口与三处由修复动作自身引入的新缺陷。*
